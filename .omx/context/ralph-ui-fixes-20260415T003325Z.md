# Ralph Context Snapshot

- Task statement:
  - Fix four project issues: delayed conversation rendering after sending from session history, untranslated English UI copy, file-tree copy menu duplication, and incorrect usage statistics by referencing `code-referance/mossx`.
- Desired outcome:
  - Existing-session follow-up messages stream into the message list immediately.
  - Core titles/descriptions touched by this work are shown in Chinese.
  - File-tree context menu exposes one top-level `复制` item with nested copy actions.
  - `计量统计` shows correct locally computed data and aligns with the intended reference behavior.
- Known facts/evidence:
  - Repo has no existing `.omx/plans` artifacts, so Ralph planning prerequisites must be created.
  - `frontend/src/hooks/useSessionSend.ts` sends follow-up messages but only toggles a local `isSending` flag.
  - `frontend/src/hooks/useConversationHistory/useConversationHistory.ts` maintains cached entries and execution-process patches.
  - `frontend/src/components/NormalizedConversation/LoadingCard.tsx` and `frontend/src/components/kanban/KanbanUsageDashboard.tsx` contain mojibake / non-Chinese UI strings.
  - `frontend/src/components/file-tree/FileTreePanel.tsx` currently renders separate actions for duplicate, relative path copy, and absolute path copy.
  - Usage dashboard data comes from `frontend/src/lib/api/localUsage.ts` calling Tauri command `get_project_usage_statistics`.
- Constraints:
  - No new dependencies.
  - Keep diffs small and reversible.
  - Verify with lint/typecheck/tests after changes.
  - Do not edit generated shared types unless generation is required.
- Unknowns/open questions:
  - Exact reason the streaming entries are hidden until hook completion.
  - Whether usage-statistics error is frontend aggregation/display, backend command logic, or both.
  - Which remaining English labels are in scope beyond the touched modules.
- Likely codebase touchpoints:
  - `frontend/src/hooks/useConversationHistory/useConversationHistory.ts`
  - `frontend/src/hooks/useSessionSend.ts`
  - `frontend/src/components/kanban/KanbanSessionConversationView.tsx`
  - `frontend/src/components/NormalizedConversation/*`
  - `frontend/src/components/file-tree/FileTreePanel.tsx`
  - `frontend/src/components/kanban/KanbanUsageDashboard.tsx`
  - `frontend/src/lib/api/localUsage.ts`
  - Backend Tauri/local usage command implementation under `crates/`

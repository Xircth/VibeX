# Ralph Context Snapshot

- task statement:
  修复 3 个前端 UI 缺陷：
  1. 会话执行区点击停止后，Loading 文案仍显示“AI 正在思考”，应改为“正在停止 Hook...”
  2. Kanban 会话列表中新建会话表单里，连续点击“工作区”两次会关闭整个表单，而不是仅收起工作区下拉。
  3. 会话消息输入框里把模型切到 `GPT-5.4` 后，会被回退到 `Default`。
- desired outcome:
  停止态文案准确；新建会话表单中的工作区选择器不会误关外层 Popover；Codex 模型选择能稳定保留 `GPT-5.4`。
- known facts/evidence:
  - `.omx/plans/prd-ralph-ui-fixes.md` 和 `.omx/plans/test-spec-ralph-ui-fixes.md` 已存在。
  - `DisplayConversationEntry.tsx` 当前在 `isStopping` 时隐藏 loading 行，而 `LoadingCard.tsx` 只会显示“AI 正在思考...”。
  - `SessionHubSidebar.tsx` 使用 `Popover` 包裹新建会话表单，内部“工作区”使用 Radix `Select`；`PopoverContent` 已做部分 `onInteractOutside` 防护。
  - `TerminalProfileControls.tsx` 通过 `getCodexVariantFromConfigSelection()` 将 UI 选择映射回 profile variant。
  - `getCodexVariantFromConfigSelection()` 当前优先精确匹配，失败后按 `model/sandbox/approval/reasoning` 打分回退，可能把 `gpt-5.4` 选择映射回 `DEFAULT`。
- constraints:
  - 工作树已存在大量未提交改动，不能回滚无关修改。
  - 不新增依赖。
  - 优先小范围、可回滚修复，并补前端测试。
- unknowns/open questions:
  - 当前 profile 配置中是否一定存在 `gpt-5.4` 的精确 variant；如果不存在，需要定义合理回退策略。
  - 新建会话 `Select` 的第二次点击是否来自触发器本身的 outside-interaction 判定，还是 portal 容器导致的焦点外泄。
- likely codebase touchpoints:
  - `frontend/src/components/NormalizedConversation/DisplayConversationEntry.tsx`
  - `frontend/src/components/NormalizedConversation/LoadingCard.tsx`
  - `frontend/src/components/kanban/session-hub/SessionHubSidebar.tsx`
  - `frontend/src/components/tasks/TerminalProfileControls.tsx`
  - `frontend/src/utils/executor.ts`
  - `frontend/src/utils/executor.test.ts`

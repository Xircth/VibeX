# VibeX V0.1.0 发布文案

VibeX V0.1.0 聚焦桌面端 Agent 工作流的稳定性、可视化与交互体验。本版本围绕 Kanban 会话执行、ACP 消息渲染、命令菜单、上下文管理、Dev 预览和终端体验做了系统性优化，让多 Agent 项目开发流程更接近一个可持续使用的桌面工作台。

## 主要更新

- 优化 Kanban 会话执行区与监控区的状态保持，修复会话切换、工作区切换后消息流消失或重复叠加的问题。
- 改进 ACP 消息渲染，支持更清晰的 Plan、终端输出、上下文压缩状态、图片与 Markdown 内容展示。
- 新增 AI 消息默认折叠体验，可保留最终输出，将中间过程按需折叠查看。
- 优化 Codex 相关能力，加入目标状态、上下文用量、套餐额度入口与命令展示体验。
- 重构 `/`、`#`、`@`、`$` 等输入命令菜单，让菜单从输入框上方弹出，并简化命令分组与展示。
- 优化 Dev 服务器预览代理，修复浏览器可访问但 App 预览无法访问的问题，并提升本地资源加载速度。
- 改进“选择元素作为内容”的预览桥接链路，保持准确的组件定位与源码栈信息。
- 修复终端创建、终端类型选择、会话删除、监控取消、Toast 行为和多处 Kanban UI 细节问题。
- 调整设置页、Kanban 页和会话卡片视觉细节，减少渐变背景与不一致状态色。

## 推荐升级理由

如果你主要使用 VibeX 管理 Codex、Claude Code 或 OpenCode 会话，本版本会明显改善长会话、多轮任务、Dev 预览、上下文压缩和会话监控的体验。尤其是会话消息重复渲染、预览页加载失败、命令菜单位置异常、终端启动延迟等高频问题，本版本都进行了集中修复。

## 已知说明

- Dev 预览仍会优先通过本地代理注入组件选择脚本；如果代理探测失败，会回退到原始本地地址以保证页面可打开。
- “选择元素作为内容”保持完整组件栈解析，不通过牺牲定位准确性换取速度。
- 部分 Agent 原生命令是否有输出，仍取决于对应 ACP/CLI 本身的支持能力。

## 发布标题

VibeX V0.1.0: 更稳定的 Agent 桌面工作台

## 简短介绍

VibeX V0.1.0 现已发布。本版本重点提升 Kanban 会话执行、ACP 消息渲染、输入命令、上下文管理、Dev 预览与终端体验，修复多轮会话重复渲染、预览访问失败、命令菜单异常和终端启动延迟等问题，让本地 Agent 开发工作流更稳定、更清晰。

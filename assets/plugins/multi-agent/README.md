---
summary: 让父 Agent 把子任务委托给其它 Agent。
---

# 多智能体协同

启用后，Host 在之后新开或重新绑定的 Agent 会话中注入 `vibex-delegation-mcp`。输入框可以用 `&` 点名其它 Agent；父模型通过 `delegate_to_agent` 真正启动子任务。

插件启停就是委托启停。深度、结果缓存和子智能体会话配置只在本页生效，且只作用于委托子会话。

禁用后 `&` 立即消失。已经注入的会话会把工具清单留到该会话结束，但新的委托会被拒绝。

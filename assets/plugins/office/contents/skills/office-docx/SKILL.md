---
name: office-docx
description: Create and modify DOCX artifacts with VibeX's locked OfficeCLI Runtime.
---

# Office DOCX

Use the exact content-addressed `officecli` Runtime supplied by VibeX. Never
install or resolve another copy from the user's shell PATH. Keep document
structure and styles stable unless the request changes them, validate the
result, and return the workspace-relative DOCX path.

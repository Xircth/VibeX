---
name: office-docx
description: Create and modify DOCX artifacts with the managed OfficeCLI tool.
---

# Office DOCX

Use the managed `officecli` executable supplied by VibeX. Keep document
structure and styles stable unless the request changes them, validate the
result, and return the workspace-relative DOCX path.

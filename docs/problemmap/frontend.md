# ProblemMap: Frontend

Scope: `frontend/src/**`, excluding generated assets and dependencies.

## Problems

### FE-001: Dockview group policy is duplicated across layout and action context

- Category: duplication, weak boundary, missing tests
- Severity: high
- Confidence: high
- Files: `frontend/src/components/layout/IDELayout.tsx`, `frontend/src/contexts/PanelActionsContext.tsx`
- Evidence: both files define overlapping group classification and editor-group helper logic, including `LEFT_PANEL_IDS`, `isLeftGroup`, `isBottomGroup`, `isEditorGroup`, `compareEditorGroups`, and `getNextEditorGroupId`.
- Risk: Dockview recovery, terminal group recreation, and editor group preservation can drift between the layout owner and the action API.
- Suggested behavior lock: unit tests for group classification and editor ordering, plus one integration test for terminal-group recreation and editor preservation after layout restore/remove flows.
- Cleanup status: fixed in this pass.
- Fix: extracted `frontend/src/utils/dockviewGroupPolicy.ts` and made `IDELayout.tsx` plus `PanelActionsContext.tsx` consume the same panel/group policy helpers.
- Behavior lock: added `frontend/src/utils/dockviewGroupPolicy.test.ts` for left/bottom/editor classification, placeholder split exclusion, editor group ordering, and next editor group id selection.
- Verification: `pnpm vitest run src/utils/dockviewGroupPolicy.test.ts`; `pnpm run check`; `pnpm run frontend:lint`.

### FE-002: Task follow-up composer is a god component and state machine

- Category: complex implementation, weak boundary, missing tests
- Severity: high
- Confidence: high
- Files: `frontend/src/components/tasks/TaskFollowUpSection.tsx`, `frontend/src/components/tasks/follow-up/SessionComposerInput.tsx`
- Evidence: the component combines workspace/session resolution, attempt stop state, git summary, scratch persistence, queueing/cancel, image upload, prompt enhancement, hotkey scopes, context compacting, and send flow in one large component.
- Risk: user-send behavior, cancellation, scratch persistence, and context handling are hard to change independently.
- Suggested behavior lock: tests around send/cancel transitions, scratch persistence, and prompt/image inputs before splitting responsibilities.
- Verification blocker fixed: `SessionComposerInput` tag typeahead now normalizes nullable tag content to `undefined` before constructing `ComposerTypeaheadOption`, matching the menu option contract and restoring `frontend:check`.
- Cleanup status: pass 1 completed for the session-composer typeahead option boundary; pass 2 completed for draft scratch data normalization/payload construction; pass 3 completed for pure send/queue/compact decision extraction; pass 4 completed for image attachment derivation/merge policy; pass 5 completed for prompt enhancement request/result policy; pass 6 routed prompt enhancement error handling through the tested clean helper; pass 7 deleted the unreachable legacy prompt-error mapper body; pass 8 extracted pure context compact turn/pending-process/error policy; pass 9 extracted pure queue snapshot/attach-seed/refresh policy; pass 10 extracted pure editor-change and image remove/clear state policy; pass 11 extracted pure scratch/executor profile hydration decisions; pass 12 extracted pure default executor profile source priority; pass 13 extracted pure after-send local cleanup policy; pass 14 extracted pure uploaded-image application policy; pass 15 extracted pure pending tool approval detection; pass 16 extracted pure git-summary repo/file-count derivation; pass 17 extracted pure conflict-context derivation; pass 18 extracted pure workspace/session/selector-label derivation; pass 19 extracted pure attempt-stopped branch refresh decision; pass 20 extracted pure hotkey scope activation derivation; pass 21 extracted pure draft scratch hydration decision; pass 22 extracted pure executor-profile autosave decision; pass 23 extracted pure prompt enhancement start decision; pass 24 extracted pure selected-session notification derivation; pass 25 extracted pure created-session profile memory decision; pass 26 extracted pure session-rename invalidation derivation; pass 27 extracted pure prompt enhancement enablement derivation; pass 28 extracted pure composer editability derivation; pass 29 extracted pure scratch target derivation; pass 30 extracted pure compacting-state derivation; pass 31 extracted pure queue indicator derivation; pass 32 extracted pure conflict action derivation; pass 33 extracted pure topbar visibility derivation; pass 34 extracted pure changed-file summary visibility derivation; pass 35 extracted pure todo-list presentation derivation; pass 36 extracted queue query-key and mutation-input boundaries; pass 37 collapsed duplicate image-upload queue seed derivation; pass 38 extracted queue runtime query/mutation side effects; pass 39 extracted prompt enhancement runtime side effects; pass 40 extracted image upload runtime side effects; pass 41 extracted context compact runtime side effects; pass 42 extracted session rename runtime side effects; pass 43 extracted attempt-stopped branch refresh runtime side effects; pass 44 extracted draft scratch persistence runtime side effects; pass 45 extracted executor-profile hydration/autosave runtime side effects; pass 46 extracted draft hydration/after-send cleanup runtime side effects; pass 47 moved queue status refresh runtime side effects into the queue hook; pass 48 extracted session selection/created-session callback runtime side effects; pass 49 extracted hotkey scope activation runtime side effects; pass 50 extracted editor-change queue/error/draft-sync runtime side effects; pass 51 extracted submit/queue runtime side effects; pass 52 extracted image removal runtime side effects; remaining non-prompt/non-upload/non-compact/non-rename/non-attempt-refresh/non-scratch-persistence/non-profile-runtime/non-draft-runtime/non-queue-refresh/non-session-callback/non-hotkey-runtime/non-editor-runtime/non-submit-runtime/non-image-removal-runtime side-effect orchestration still needs cleanup.
- Cleanup status update: pass 53 moved compact eligibility/action closure into the context compact hook; remaining non-prompt/non-upload/non-compact/non-rename/non-attempt-refresh/non-scratch-persistence/non-profile-runtime/non-draft-runtime/non-queue-refresh/non-session-callback/non-hotkey-runtime/non-editor-runtime/non-submit-runtime/non-image-removal-runtime/non-compact-handler-runtime side-effect orchestration still needs cleanup.
- Cleanup status update: pass 54 extracted composer focus/blur runtime state; remaining non-prompt/non-upload/non-compact/non-rename/non-attempt-refresh/non-scratch-persistence/non-profile-runtime/non-draft-runtime/non-queue-refresh/non-session-callback/non-hotkey-runtime/non-editor-runtime/non-submit-runtime/non-image-removal-runtime/non-compact-handler-runtime/non-focus-runtime side-effect orchestration still needs cleanup.
- Cleanup status update: pass 55 extracted composer local message/image/ref/profile-selection state; remaining FE-002 cleanup should focus on presentational composition and any newly found duplicated/weak boundaries rather than React effect/callback orchestration.
- Cleanup status update: pass 56 extracted the todo-list button presentation component; remaining FE-002 cleanup should continue with topbar/input/action-bar composition and prop surface review.
- Cleanup status update: pass 57 extracted the composer topbar presentation component; remaining FE-002 cleanup should continue with input/action-bar prop surface review.
- Cleanup status update: pass 58 removed the hidden duplicate todo popover from `ActionBar`; remaining FE-002 cleanup should focus on input/action-bar prop surface only.
- Cleanup status update: pass 59 reused the shared follow-up content predicate in `ActionBar`; remaining FE-002 cleanup should focus on input/action-bar prop surface only.
- Cleanup status update: pass 60 extracted the `ActionBar` image attachment control; remaining FE-002 cleanup should continue with input/action-bar prop surface only.
- Cleanup status update: pass 61 extracted the `ActionBar` utility buttons and fixed the compact-context button label from mojibake to `压缩上下文`; remaining FE-002 cleanup should continue with running/send control prop surface only.
- Cleanup status update: pass 62 extracted the `ActionBar` running queue/cancel/stop controls and fixed loading-state accessible names; remaining FE-002 cleanup should continue with idle send/clear controls and `SessionComposerInput` prop surface only.
- Cleanup status update: pass 63 extracted the `ActionBar` idle clear/send controls and fixed send loading-state accessible names; remaining FE-002 cleanup should focus on `SessionComposerInput` prop surface only.
- Cleanup status update: pass 64 grouped `SessionComposerInput` shortcut/task/workspace/repo/project/executor context into a single prop and added direct component behavior locks; the previously identified FE-002 parent/actionbar/input prop-surface cleanup is closed, and any further FE-002 work should come from a fresh review of `SessionComposerInput` internals.
- Cleanup plans: [fe-002-composer-cleanup-plan.md](fe-002-composer-cleanup-plan.md), [fe-002-draft-cleanup-plan.md](fe-002-draft-cleanup-plan.md), [fe-002-submit-cleanup-plan.md](fe-002-submit-cleanup-plan.md), [fe-002-image-cleanup-plan.md](fe-002-image-cleanup-plan.md), [fe-002-prompt-cleanup-plan.md](fe-002-prompt-cleanup-plan.md), [fe-002-compact-cleanup-plan.md](fe-002-compact-cleanup-plan.md), [fe-002-queue-cleanup-plan.md](fe-002-queue-cleanup-plan.md), [fe-002-editor-image-state-cleanup-plan.md](fe-002-editor-image-state-cleanup-plan.md), [fe-002-scratch-profile-cleanup-plan.md](fe-002-scratch-profile-cleanup-plan.md), [fe-002-profile-source-cleanup-plan.md](fe-002-profile-source-cleanup-plan.md), [fe-002-after-send-cleanup-plan.md](fe-002-after-send-cleanup-plan.md), [fe-002-image-upload-application-plan.md](fe-002-image-upload-application-plan.md), [fe-002-pending-approval-cleanup-plan.md](fe-002-pending-approval-cleanup-plan.md), [fe-002-git-summary-cleanup-plan.md](fe-002-git-summary-cleanup-plan.md), [fe-002-conflict-context-cleanup-plan.md](fe-002-conflict-context-cleanup-plan.md), [fe-002-session-derivation-cleanup-plan.md](fe-002-session-derivation-cleanup-plan.md), [fe-002-attempt-refresh-cleanup-plan.md](fe-002-attempt-refresh-cleanup-plan.md), [fe-002-hotkey-scope-cleanup-plan.md](fe-002-hotkey-scope-cleanup-plan.md), [fe-002-draft-hydration-cleanup-plan.md](fe-002-draft-hydration-cleanup-plan.md), [fe-002-profile-autosave-cleanup-plan.md](fe-002-profile-autosave-cleanup-plan.md), [fe-002-prompt-start-cleanup-plan.md](fe-002-prompt-start-cleanup-plan.md), [fe-002-session-selection-cleanup-plan.md](fe-002-session-selection-cleanup-plan.md), [fe-002-created-session-profile-cleanup-plan.md](fe-002-created-session-profile-cleanup-plan.md), [fe-002-session-rename-invalidation-cleanup-plan.md](fe-002-session-rename-invalidation-cleanup-plan.md), [fe-002-prompt-enable-cleanup-plan.md](fe-002-prompt-enable-cleanup-plan.md), [fe-002-editability-cleanup-plan.md](fe-002-editability-cleanup-plan.md), [fe-002-scratch-target-cleanup-plan.md](fe-002-scratch-target-cleanup-plan.md), [fe-002-compact-state-cleanup-plan.md](fe-002-compact-state-cleanup-plan.md), [fe-002-queue-indicator-cleanup-plan.md](fe-002-queue-indicator-cleanup-plan.md), [fe-002-conflict-action-cleanup-plan.md](fe-002-conflict-action-cleanup-plan.md), [fe-002-topbar-visibility-cleanup-plan.md](fe-002-topbar-visibility-cleanup-plan.md), [fe-002-git-summary-visibility-cleanup-plan.md](fe-002-git-summary-visibility-cleanup-plan.md), [fe-002-todo-list-cleanup-plan.md](fe-002-todo-list-cleanup-plan.md), [fe-002-queue-mutation-cleanup-plan.md](fe-002-queue-mutation-cleanup-plan.md), [fe-002-image-upload-queue-seed-cleanup-plan.md](fe-002-image-upload-queue-seed-cleanup-plan.md), [fe-002-queue-runtime-hook-cleanup-plan.md](fe-002-queue-runtime-hook-cleanup-plan.md), [fe-002-prompt-runtime-hook-cleanup-plan.md](fe-002-prompt-runtime-hook-cleanup-plan.md), [fe-002-image-upload-runtime-hook-cleanup-plan.md](fe-002-image-upload-runtime-hook-cleanup-plan.md), [fe-002-compact-runtime-hook-cleanup-plan.md](fe-002-compact-runtime-hook-cleanup-plan.md), [fe-002-session-rename-runtime-hook-cleanup-plan.md](fe-002-session-rename-runtime-hook-cleanup-plan.md), [fe-002-attempt-refresh-runtime-hook-cleanup-plan.md](fe-002-attempt-refresh-runtime-hook-cleanup-plan.md), [fe-002-scratch-persistence-runtime-hook-cleanup-plan.md](fe-002-scratch-persistence-runtime-hook-cleanup-plan.md), [fe-002-profile-runtime-hook-cleanup-plan.md](fe-002-profile-runtime-hook-cleanup-plan.md), [fe-002-draft-runtime-hook-cleanup-plan.md](fe-002-draft-runtime-hook-cleanup-plan.md), [fe-002-queue-refresh-runtime-hook-cleanup-plan.md](fe-002-queue-refresh-runtime-hook-cleanup-plan.md), [fe-002-session-callback-runtime-hook-cleanup-plan.md](fe-002-session-callback-runtime-hook-cleanup-plan.md), [fe-002-hotkey-runtime-hook-cleanup-plan.md](fe-002-hotkey-runtime-hook-cleanup-plan.md), [fe-002-editor-runtime-hook-cleanup-plan.md](fe-002-editor-runtime-hook-cleanup-plan.md), [fe-002-submit-runtime-hook-cleanup-plan.md](fe-002-submit-runtime-hook-cleanup-plan.md), [fe-002-image-removal-runtime-hook-cleanup-plan.md](fe-002-image-removal-runtime-hook-cleanup-plan.md).
- Cleanup plan update: [fe-002-compact-handler-runtime-hook-cleanup-plan.md](fe-002-compact-handler-runtime-hook-cleanup-plan.md), [fe-002-actionbar-attachment-control-cleanup-plan.md](fe-002-actionbar-attachment-control-cleanup-plan.md), [fe-002-actionbar-utility-buttons-cleanup-plan.md](fe-002-actionbar-utility-buttons-cleanup-plan.md), [fe-002-actionbar-running-controls-cleanup-plan.md](fe-002-actionbar-running-controls-cleanup-plan.md).
- Cleanup plan update: [fe-002-focus-runtime-hook-cleanup-plan.md](fe-002-focus-runtime-hook-cleanup-plan.md).
- Cleanup plan update: [fe-002-local-state-hook-cleanup-plan.md](fe-002-local-state-hook-cleanup-plan.md).
- Cleanup plan update: [fe-002-todo-button-component-cleanup-plan.md](fe-002-todo-button-component-cleanup-plan.md).
- Cleanup plan update: [fe-002-topbar-component-cleanup-plan.md](fe-002-topbar-component-cleanup-plan.md).
- Cleanup plan update: [fe-002-actionbar-dead-todo-cleanup-plan.md](fe-002-actionbar-dead-todo-cleanup-plan.md).
- Cleanup plan update: [fe-002-actionbar-content-gate-cleanup-plan.md](fe-002-actionbar-content-gate-cleanup-plan.md).
- Cleanup plan update: [fe-002-actionbar-idle-controls-cleanup-plan.md](fe-002-actionbar-idle-controls-cleanup-plan.md).
- Cleanup plan update: [fe-002-session-composer-input-context-cleanup-plan.md](fe-002-session-composer-input-context-cleanup-plan.md).
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerTypeaheadOptions.ts` so slash command, dollar command, tag/file search result, and root directory option derivation are pure and directly testable instead of embedded in `SessionComposerInput.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerDraft.ts` so draft payload extraction, legacy executor profile normalization, empty-draft persistence policy, scratch update payload construction, executor profile keying, and scratch/profile hydration decisions are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerSubmit.ts` so typing/send/compact eligibility, submit shortcut action selection, content detection, and queued follow-up prompt construction are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerImages.ts` so stored path hydration, uploaded-image attachment construction, queued/current/new attachment merging, replaced-preview revoke selection, image removal, and image clearing are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerPromptEnhancement.ts` so prompt enhancement request construction, enhanced-prompt result normalization, and prompt enhancement error mapping are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`; the component now imports the tested clean mapper directly and no longer carries the unreachable legacy mapper body.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerCompact.ts` so context compact turn input construction, provider runtime process id extraction, pending process clearing, running compact detection, and compact error message formatting are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerQueue.ts` so queue snapshot extraction, visible queued-message gating, attach-image queue seed derivation, editor-change side-effect decisions, and process-count refresh policy are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerDraft.ts` so default executor profile source priority is pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSubmit.ts` so after-send local cleanup state is pure and directly testable instead of embedded in the `useFollowUpSend` cleanup callback.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerImages.ts` so successful upload application against queued/current attachments is pure and directly testable instead of embedded in `handleAttachImages`.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSubmit.ts` so pending tool approval detection is pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerGitSummary.ts` so summary repo selection and changed-file counting are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerConflicts.ts` so conflict repo selection and conflict-resolution instruction construction are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerSession.ts` so workspace id precedence, active session id suppression, and selector labels are pure and directly testable instead of embedded in `TaskFollowUpSection.tsx`.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerAttempt.ts` so the attempt-stopped branch refresh decision is pure and directly testable instead of embedded in a React effect.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerHotkeys.ts` so hotkey scope activation is pure and directly testable instead of duplicated inside React effects.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerDraft.ts` so draft scratch hydration eligibility and payload derivation are pure and directly testable instead of embedded in a React effect.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerDraft.ts` so executor-profile autosave key progression and save gating are pure and directly testable instead of embedded in a React effect.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerPromptEnhancement.ts` so prompt enhancement start gating is pure and directly testable instead of embedded in the async handler.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSession.ts` so selected-session parent notification payload derivation is pure and directly testable instead of embedded in the UI callback.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSession.ts` so created-session executor profile memory updates are pure and directly testable instead of embedded in a ref-mutating callback.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSession.ts` so session-rename query invalidation keys are pure and directly testable instead of embedded in the rename callback.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerPromptEnhancement.ts` so prompt enhancement enablement is pure and directly testable instead of embedded in the component render state.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSubmit.ts` so composer editability is pure and directly testable instead of embedded in component render state.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSession.ts` so draft scratch target selection is pure and directly testable instead of embedded in component state setup.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerCompact.ts` so compacting-state derivation is pure and directly testable instead of embedded in component render state.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerQueue.ts` so queue indicator visibility, preview, and attachment count are pure and directly testable instead of embedded in component render state.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerConflicts.ts` so conflict resolve/abort action gates are pure and directly testable instead of embedded in JSX.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerSession.ts` so topbar visibility is pure and directly testable instead of embedded in JSX.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerGitSummary.ts` so changed-file summary visibility is pure and directly testable instead of embedded in JSX.
- Fix: extracted `frontend/src/components/tasks/follow-up/sessionComposerTodos.ts` so todo-list count visibility and todo status presentation are pure and directly testable instead of embedded in JSX.
- Fix: extended `frontend/src/components/tasks/follow-up/sessionComposerQueue.ts` so queue status query keys plus queue/cancel mutation inputs are pure and directly testable; `TaskFollowUpSection.tsx` no longer uses `sessionId!` for queue mutations.
- Fix: collapsed duplicate image-upload queue seed derivation so `TaskFollowUpSection.tsx` now uses the single `getUploadedImageApplication` result for queue cancellation, scratch message hydration, preview revocation, and scratch image-path saving.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerQueue.ts` so queue status querying, queue/cancel mutations, optimistic queue-status cache writes, `queueMessage`, `cancelQueue`, queue loading, and queue indicator derivation are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerPromptEnhancement.ts` so enhancement busy state, start gating, API execution, request construction, response normalization, error mapping, and enhanced prompt application are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerImageUpload.ts` so image upload API execution, queue-cache reads, queued-message cancellation/restoration, scratch image persistence, and duplicate preview revocation are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerContextCompact.ts` so compact runtime sending, pending process state, process-list clearing, timeout clearing, and error mapping are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerSessionRename.ts` so rename API execution and workspace/session query invalidation are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerAttemptRefresh.ts` so previous-running tracking and branch-state refresh side effects are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerDraftScratch.ts` so draft scratch loading, scratch-data extraction, scratch executor-profile extraction, attached-image path tracking, non-blocking scratch persistence, and debounced draft message persistence are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerExecutorProfileHydration.ts` so scratch-id profile reset, default profile hydration, scratch executor-profile application, latest-profile ref synchronization, and profile autosave de-duplication are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerDraftHydration.ts` so draft scratch hydration, stored image attachment hydration, preview revocation, after-send local cleanup, hydrated scratch id advancement, and scratch deletion are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: extended `frontend/src/components/tasks/follow-up/useSessionComposerQueue.ts` so session-id and process-count queue-status refresh effects are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerSessionCallbacks.ts` so selected-session parent notifications and created-session executor-profile memory are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerHotkeys.ts` so hotkey scope enable/disable lifecycle side effects are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerEditorChange.ts` so editor-change queue cancellation, error clearing, local message update, and draft-persistence setter freshness are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerSubmitActions.ts` so queued follow-up runtime orchestration and submit shortcut send-vs-queue routing are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerImageRemoval.ts` so image removal state mutation, preview revocation, and scratch image-path persistence are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: updated `frontend/src/components/tasks/follow-up/useSessionComposerContextCompact.ts` so compact eligibility and the compact action closure live together in the hook instead of leaving a wrapper callback in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerFocus.ts` so composer focus/blur state and same-surface blur preservation are no longer embedded in `TaskFollowUpSection.tsx` JSX handlers.
- Fix: added `frontend/src/components/tasks/follow-up/useSessionComposerLocalState.ts` so draft text, attached image state/path derivation, executor profile ref, and profile selection fallback are no longer embedded as direct `useState`/`useRef` calls in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/TodoListButton.tsx` so todo count, empty-state popover, and status marker rendering are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/SessionComposerTopbar.tsx` so diff summary, goal/token/todo controls, jump control, and session-selector composition are no longer embedded in `TaskFollowUpSection.tsx`.
- Fix: removed the hidden duplicate todo popover from `frontend/src/components/tasks/follow-up/ActionBar.tsx` and dropped the unused `todos` prop from the action-bar boundary.
- Fix: updated `frontend/src/components/tasks/follow-up/ActionBar.tsx` to reuse `hasFollowUpContent` for queue-button content gating instead of duplicating the submit-helper predicate.
- Fix: added `frontend/src/components/tasks/follow-up/ActionBarImageButton.tsx` so the hidden image file input, paperclip trigger, image-only filtering, and input reset are no longer embedded in `ActionBar.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/ActionBarUtilityButtons.tsx` so compact-context and prompt-enhancement utility button presentation, loading icons, labels, and availability gates are no longer embedded in `ActionBar.tsx`; the compact button title/aria-label is now the correct `压缩上下文` text.
- Fix: added `frontend/src/components/tasks/follow-up/ActionBarRunningControls.tsx` so queued-message, cancel-queue, and stop-execution button presentation, disabled gates, loading icons, and stable accessible names are no longer embedded in `ActionBar.tsx`.
- Fix: added `frontend/src/components/tasks/follow-up/ActionBarIdleControls.tsx` so review-clear visibility, send disabled gates, conflict/send label selection, loading icon rendering, and stable send accessible names are no longer embedded in `ActionBar.tsx`.
- Fix: added `SessionComposerInputContext` in `frontend/src/components/tasks/follow-up/SessionComposerInput.tsx` and updated `TaskFollowUpSection` to pass shortcut/task/workspace/repo/project/executor context as one named object instead of a flat optional prop set.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerTypeahead.test.ts` to cover slash ordering, dollar command options, tag/file option mapping, nullable tag descriptions, root entry limiting/sorting, and trigger-specific reference filtering.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerDraft.test.ts` for current/legacy draft extraction, legacy `model_id` normalization, empty-draft skip policy, scratch update payloads, scratch-id reset/preserve behavior, default-profile hydration, and scratch executor profile single-application behavior.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerSubmit.test.ts` for queueable/sendable content detection, typing/send/compact gates, shortcut action selection, and queued follow-up prompt construction.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerImages.test.ts` for stored image path hydration, uploaded response conversion, queue/current/upload merge ordering, duplicate replacement, preview revocation selection, image removal, and image clearing.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerPromptEnhancement.test.ts` for request payload construction, enhanced-prompt trimming, empty result rejection, backend prefix stripping, and known error classification.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerCompact.test.ts` for compact turn construction, runtime process id extraction, pending-process clearing, running compact detection, and compact error fallback.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerQueue.test.ts` for queue snapshot extraction, visible queued-message gating, image-attach queue seed derivation, editor-change side-effect decisions, and queue refresh policy.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerDraft.test.ts` for scratch/latest-process/created-session/session/config/available-profile/default-null executor profile source priority.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSubmit.test.ts` for message reset, image clear/revoke selection, hydrated scratch id update, and scratch deletion gating after a successful send.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerImages.test.ts` for queued-state recovery, uploaded duplicate replacement, preview revocation selection, and scratch image-path derivation after a successful upload.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSubmit.test.ts` for normalized pending tool approval detection and non-blocking completed/non-tool/non-normalized entries.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerGitSummary.test.ts` for selected-repo fallback and staged/unstaged path deduplication.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerConflicts.test.ts` for rebase/conflicted repo selection, empty-conflict suppression, and populated conflict prompt construction.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerSession.test.ts` for workspace id source priority, new-session session id suppression, selector label derivation, and compact label truncation.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerAttempt.test.ts` for attempt-stopped branch refresh gating and previous-running snapshot progression.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerHotkeys.test.ts` for editable/focused hotkey scope activation and readonly/unfocused suppression.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerDraft.test.ts` for loading suppression, same-scratch suppression, new-scratch message/image hydration, and empty-draft defaults.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerDraft.test.ts` for executor-profile autosave key changes, duplicate suppression, loading suppression, and null-profile transitions.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerPromptEnhancement.test.ts` for busy-state suppression and whitespace-only draft suppression before enhancement starts.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSession.test.ts` for selected-session parent notification payload creation and missing-workspace suppression.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSession.test.ts` for created-session profile memory updates and null/executor-less suppression.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSession.test.ts` for session-rename workspace/session query invalidation keys and missing-workspace suppression.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerPromptEnhancement.test.ts` for prompt enhancement enablement with disabled typing and whitespace-only drafts.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSubmit.test.ts` for retry-active and pending-approval editability gates.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSession.test.ts` for new-session workspace scratch targets, existing-session scratch targets, and missing-id suppression.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerCompact.test.ts` for pending compact id and running compact process combined state.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerQueue.test.ts` for queue indicator visibility, hidden preview, and attachment count suppression.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerConflicts.test.ts` for resolve editability, abort availability, and running-attempt suppression.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerSession.test.ts` for topbar visibility from token usage, goal state, session selector availability, and executor profile.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerGitSummary.test.ts` for changed-file summary visibility on positive and zero file counts.
- Behavior lock: added `frontend/src/components/tasks/follow-up/sessionComposerTodos.test.ts` for empty/count visibility, completed presentation, both running status spellings, and cancelled content styling.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/sessionComposerQueue.test.ts` for queue query-key shape, guarded queue mutation inputs, and guarded cancel mutation inputs.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerQueue.test.tsx` for queue-status loading, queue mutation API payloads, queue/cancel cache writes, and missing-session mutation suppression.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerPromptEnhancement.test.tsx` for empty-draft suppression, enhancement request payloads, response-object normalization, error clearing, and mapped failure messages.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerImageUpload.test.tsx` for missing-workspace upload suppression, upload API payloads, queued-message cancellation/restoration, scratch image-path persistence, and duplicate preview revocation.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerContextCompact.test.tsx` for unavailable-input suppression, provider runtime payloads, pending-process state, process-list clearing, timeout clearing, and provider failure mapping.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerSessionRename.test.tsx` for rename API payloads, workspace/session invalidation, missing-workspace invalidation suppression, and failed-rename invalidation suppression.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerAttemptRefresh.test.tsx` for running-to-stopped refreshes, missing-workspace suppression, duplicate stopped-state suppression, and repeated start/stop cycles.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerDraftScratch.test.tsx` for scratch loading, draft extraction, missing-workspace suppression, empty-draft suppression, attached-image path freshness, debounced latest-profile persistence, and non-blocking write failure logging.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerExecutorProfileHydration.test.tsx` for default selection reset, scratch-id variant preservation while loading, scratch profile single-application, latest profile ref sync, and autosave key de-duplication.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerDraftHydration.test.tsx` for loading suppression, once-per-scratch hydration, stored image hydration, preview revocation, after-send cleanup, and scratch deletion gating.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/UseSessionComposerQueue.test.tsx` for selected-session queue refreshes, process-count refreshes while running, stopped-attempt refreshes, and missing-workspace suppression.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerSessionCallbacks.test.tsx` for session selection, missing-workspace notification suppression, created-session profile memory, executor-less profile suppression, and parent callback forwarding.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerHotkeys.test.tsx` for active scope enablement, inactive scope disablement, cleanup disablement, and active-to-inactive rerender behavior.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerEditorChange.test.tsx` for queued draft cancellation, local-message update, draft persistence sync, error clearing, non-queued suppression, and latest draft setter freshness after rerender.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerSubmitActions.test.tsx` for explicit queueing, empty/no-profile suppression, non-running shortcut send, running shortcut queueing, and already-queued shortcut suppression.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerImageRemoval.test.tsx` for matching removal, preview revocation, remaining-path scratch persistence, no-match persistence, and current executor profile ref usage.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/UseSessionComposerContextCompact.test.tsx` to call `handleCompactContext()` without arguments and verify hook-owned compact eligibility sends the provider runtime compact turn.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerFocus.test.tsx` for initial unfocused state, focus activation, same-surface blur preservation, and outside blur deactivation.
- Behavior lock: added `frontend/src/components/tasks/follow-up/UseSessionComposerLocalState.test.tsx` for empty draft/image defaults, attached image path derivation, explicit profile preservation across default changes, and fallback to the latest default profile when selection is cleared.
- Behavior lock: added `frontend/src/components/tasks/follow-up/TodoListButton.test.tsx` for empty-state popover rendering, count badge visibility, todo list content, and status marker classes.
- Behavior lock: added `frontend/src/components/tasks/follow-up/SessionComposerTopbar.test.tsx` for changed-file summary visibility, todo count rendering, jump-control click/disabled behavior, and session-selector gating.
- Behavior lock: added `frontend/src/components/tasks/follow-up/ActionBar.test.tsx` for absence of the hidden duplicate todo trigger and preserved send-button behavior.
- Behavior lock: expanded `frontend/src/components/tasks/follow-up/ActionBar.test.tsx` for queue-button enablement from conflict text, review text, and image attachments, plus no-content disabled state.
- Behavior lock: added `frontend/src/components/tasks/follow-up/ActionBarImageButton.test.tsx` for paperclip-to-file-input triggering, disabled suppression, image-only file dispatch, and input reset after image or non-image changes.
- Behavior lock: added `frontend/src/components/tasks/follow-up/ActionBarUtilityButtons.test.tsx` for the compact button's correct label, compact disabled/loading/click behavior, prompt-enhancement rendering, prompt disabled/loading behavior, and prompt click behavior.
- Behavior lock: added `frontend/src/components/tasks/follow-up/ActionBarRunningControls.test.tsx` for queued cancel, non-queued queue, loading/disabled queue gates, compacting suppression, stop rendering, and stop loading-state accessibility.
- Behavior lock: added `frontend/src/components/tasks/follow-up/ActionBarIdleControls.test.tsx` for clear-review visibility/click/disabled behavior, send click/disabled gates, conflict label selection, and send loading-state accessibility.
- Behavior lock: added `frontend/src/components/tasks/follow-up/SessionComposerInput.test.tsx` for textarea changes, Enter submit, ModifierEnter submit, plain-Enter suppression under ModifierEnter, and disabled submit suppression.
- Verification: `pnpm vitest run src/components/tasks/follow-up/sessionComposerTypeahead.test.ts`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerDraft.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerDraftScratch.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerSubmit.test.ts`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerImages.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerImageUpload.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerPromptEnhancement.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerPromptEnhancement.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerCompact.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerContextCompact.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerSession.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerSessionRename.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerAttempt.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerAttemptRefresh.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerQueue.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerQueue.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerTodos.test.ts`; `pnpm vitest run src/components/tasks/follow-up`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.
- Verification (profile runtime hook pass): red test first failed on missing `useSessionComposerExecutorProfileHydration`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerExecutorProfileHydration.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerDraft.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerDraftScratch.test.tsx`; `pnpm vitest run src/components/tasks/follow-up`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`.
- Verification (draft runtime hook pass): red test first failed on missing `useSessionComposerDraftHydration`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerDraftHydration.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerDraft.test.ts`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerSubmit.test.ts`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerImages.test.ts`; `pnpm vitest run src/components/tasks/follow-up`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`.
- Verification (queue refresh runtime pass): red test first failed because process-count changes did not refresh in `useSessionComposerQueue`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerQueue.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerQueue.test.ts`; `pnpm vitest run src/components/tasks/follow-up`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`.
- Verification (session callback runtime pass): red test first failed on missing `useSessionComposerSessionCallbacks`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerSessionCallbacks.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerSession.test.ts`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerDraft.test.ts`; `pnpm vitest run src/components/tasks/follow-up`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`.
- Verification (hotkey runtime hook pass): red test first failed on missing `useSessionComposerHotkeys`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerHotkeys.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerHotkeys.test.ts`; `pnpm vitest run src/components/tasks/follow-up` passed with 25 files / 126 tests; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check` reported no whitespace errors.
- Verification (editor runtime hook pass): red test first failed on missing `useSessionComposerEditorChange`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerEditorChange.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerQueue.test.ts`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerImageUpload.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerPromptEnhancement.test.tsx`; `pnpm vitest run src/components/tasks/follow-up` passed with 26 files / 129 tests; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check` reported no whitespace errors.
- Verification (submit runtime hook pass): red test first failed on missing `useSessionComposerSubmitActions`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerSubmitActions.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerSubmit.test.ts`; `pnpm vitest run src/components/tasks/follow-up` passed with 27 files / 134 tests; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check` reported no whitespace errors.
- Verification (image removal runtime hook pass): red test first failed on missing `useSessionComposerImageRemoval`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerImageRemoval.test.tsx`; `pnpm vitest run src/components/tasks/follow-up/sessionComposerImages.test.ts`; `pnpm vitest run src/components/tasks/follow-up` passed with 28 files / 137 tests; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check` reported no whitespace errors.
- Verification (compact handler runtime hook pass): red test first failed because `handleCompactContext()` did not use hook-owned compact eligibility; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerContextCompact.test.tsx` passed with 5 tests; `pnpm vitest run src/components/tasks/follow-up/sessionComposerCompact.test.ts` passed with 6 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 28 files / 138 tests; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check` reported no whitespace errors.
- Verification (focus runtime hook pass): red test first failed on missing `useSessionComposerFocus`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerFocus.test.tsx` passed with 1 test; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerHotkeys.test.tsx` passed with 4 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 29 files / 139 tests; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check` reported no whitespace errors.
- Verification (local state hook pass): red test first failed on missing `useSessionComposerLocalState`; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerLocalState.test.tsx` passed with 4 tests; `pnpm vitest run src/components/tasks/follow-up/UseSessionComposerExecutorProfileHydration.test.tsx` passed with 4 tests; `pnpm vitest run src/components/tasks/follow-up/sessionComposerDraft.test.ts` passed with 10 tests; `pnpm vitest run src/components/tasks/follow-up/sessionComposerImages.test.ts` passed with 9 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 30 files / 143 tests; first `frontend:check` caught a nullable test prop type and stale `ExecutorProfileId` import, then `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed; `git diff --check` reported no whitespace errors.
- Verification (todo button component pass): red test first failed on missing `TodoListButton`; `pnpm vitest run src/components/tasks/follow-up/TodoListButton.test.tsx` passed with 2 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 31 files / 145 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (topbar component pass): red test first failed on missing `SessionComposerTopbar`; `pnpm vitest run src/components/tasks/follow-up/SessionComposerTopbar.test.tsx` passed with 2 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 32 files / 147 tests; first `frontend:check` caught incomplete `TodoItem` and workspace-session fixtures, then `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (actionbar dead todo pass): red test first failed because the hidden duplicate todo trigger was still in the DOM; `pnpm vitest run src/components/tasks/follow-up/ActionBar.test.tsx` passed with 2 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 33 files / 149 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (actionbar content gate pass): `pnpm vitest run src/components/tasks/follow-up/ActionBar.test.tsx` passed with 4 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 33 files / 151 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (actionbar attachment control pass): red test first failed on missing `ActionBarImageButton`; `pnpm vitest run src/components/tasks/follow-up/ActionBarImageButton.test.tsx` passed with 4 tests after adjusting the file-input reset fixture to JSDOM constraints; `pnpm vitest run src/components/tasks/follow-up/ActionBar.test.tsx src/components/tasks/follow-up/ActionBarImageButton.test.tsx` passed with 8 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 34 files / 155 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (actionbar utility buttons pass): red test first failed on missing `ActionBarUtilityButtons`; `pnpm vitest run src/components/tasks/follow-up/ActionBarUtilityButtons.test.tsx` passed with 5 tests; `pnpm vitest run src/components/tasks/follow-up/ActionBar.test.tsx src/components/tasks/follow-up/ActionBarImageButton.test.tsx src/components/tasks/follow-up/ActionBarUtilityButtons.test.tsx` passed with 13 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 35 files / 160 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (actionbar running controls pass): red test first failed on missing `ActionBarRunningControls`; first implementation exposed empty accessible names while queue/cancel buttons rendered loading icons, then stable aria labels fixed that path; `pnpm vitest run src/components/tasks/follow-up/ActionBarRunningControls.test.tsx` passed with 6 tests; `pnpm vitest run src/components/tasks/follow-up/ActionBar.test.tsx src/components/tasks/follow-up/ActionBarImageButton.test.tsx src/components/tasks/follow-up/ActionBarUtilityButtons.test.tsx src/components/tasks/follow-up/ActionBarRunningControls.test.tsx` passed with 19 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 36 files / 166 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (actionbar idle controls pass): red test first failed on missing `ActionBarIdleControls`; `pnpm vitest run src/components/tasks/follow-up/ActionBarIdleControls.test.tsx` passed with 6 tests; `pnpm vitest run src/components/tasks/follow-up/ActionBar.test.tsx src/components/tasks/follow-up/ActionBarImageButton.test.tsx src/components/tasks/follow-up/ActionBarUtilityButtons.test.tsx src/components/tasks/follow-up/ActionBarRunningControls.test.tsx src/components/tasks/follow-up/ActionBarIdleControls.test.tsx` passed with 25 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 37 files / 172 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (session composer input context pass): first behavior-lock test run exposed the missing React Query provider in the test harness; `pnpm vitest run src/components/tasks/follow-up/SessionComposerInput.test.tsx` passed with 5 tests; `pnpm vitest run src/components/tasks/follow-up` passed with 38 files / 177 tests; first `pnpm run check` caught an obsolete `"CmdEnter"` test fixture that did not match generated `SendMessageShortcut`, then `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.

### FE-003: Conversation history hook hides shared runtime state at module scope

- Category: weak boundary, complex implementation, missing tests
- Severity: high
- Confidence: high
- Files: `frontend/src/hooks/useConversationHistory/useConversationHistory.ts`, `frontend/src/hooks/useConversationHistory/useConversationHistory.test.ts`
- Evidence: `conversationRuntimeByKey` and `conversationStreamSubscriptionCounter` are module-global state, while the hook also owns historic load, live stream retry, flattening, stopped-process reload, and process removal.
- Risk: cross-instance leakage and race conditions are difficult to reason about.
- Suggested behavior lock: multi-instance tests for shared runtime keys, subscription cleanup, stopped-process reload, and stream retry behavior.
- Cleanup status: completed. Pass 1 extracted the conversation runtime store and stream id generator; pass 2 extracted running snapshot staleness detection; pass 3 extracted latest token usage derivation; pass 4 extracted emit add-type derivation; pass 5 extracted script display entry construction; pass 6 extracted context compact display entry construction; pass 7 extracted coding-agent display construction; pass 8 extracted historic entry loading; pass 9 extracted reload planning; pass 10 extracted running stream orchestration; pass 11 extracted historic batch loading; pass 12 extracted removal planning. Final review kept the remaining load-timeout and lifecycle effects in the hook because they are hook-owned UX/effect sequencing rather than reusable policy.
- Cleanup plans: [fe-003-runtime-store-cleanup-plan.md](fe-003-runtime-store-cleanup-plan.md), [fe-003-snapshot-staleness-cleanup-plan.md](fe-003-snapshot-staleness-cleanup-plan.md), [fe-003-token-usage-cleanup-plan.md](fe-003-token-usage-cleanup-plan.md), [fe-003-emit-add-type-cleanup-plan.md](fe-003-emit-add-type-cleanup-plan.md), [fe-003-script-display-cleanup-plan.md](fe-003-script-display-cleanup-plan.md), [fe-003-context-compact-display-cleanup-plan.md](fe-003-context-compact-display-cleanup-plan.md), [fe-003-coding-agent-display-cleanup-plan.md](fe-003-coding-agent-display-cleanup-plan.md), [fe-003-historic-loader-cleanup-plan.md](fe-003-historic-loader-cleanup-plan.md), [fe-003-reload-plan-cleanup-plan.md](fe-003-reload-plan-cleanup-plan.md), [fe-003-running-stream-cleanup-plan.md](fe-003-running-stream-cleanup-plan.md), [fe-003-historic-batch-cleanup-plan.md](fe-003-historic-batch-cleanup-plan.md), [fe-003-removal-plan-cleanup-plan.md](fe-003-removal-plan-cleanup-plan.md).
- Fix: added `frontend/src/hooks/useConversationHistory/conversationRuntimeStore.ts`, moving the shared runtime cache, retention limit, test clear hook, runtime restore accessor, cache write policy, and stream id generator out of the React hook module.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationSnapshotStaleness.ts`, moving snapshot comparison key derivation and stale running snapshot detection out of the React hook while keeping the hook as the owner of current displayed state.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationTokenUsage.ts`, moving latest token usage derivation out of the entry emission callback while keeping the hook as the owner of `setTokenUsageInfo`.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationEmitAddType.ts`, moving ExitPlanMode add-type classification out of the entry emission callback while keeping the hook as the owner of `onEntriesUpdated`.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationScriptDisplay.ts`, moving script context label mapping, live status flags, exit status formatting, output joining, and patch-key construction out of `flattenEntriesForEmit`.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationContextCompactDisplay.ts`, moving context compact prompt detection, status text selection, system/error entry construction, and patch-key construction out of `flattenEntriesForEmit`.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationCodingAgentDisplay.ts`, moving coding-agent/review user prompt synthesis, raw user/token filtering, assistant replay-prefix stripping, pending approval detection, running loading insertion, setup-required detection, and context compact delegation out of `flattenEntriesForEmit`.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationHistoricEntriesLoader.ts`, moving historic stream loading, normalized/raw stream selection, running snapshot idle/max timeout selection, error fallback, and controller cleanup out of the hook while leaving batch and reload orchestration in the hook.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationReloadPlan.ts`, moving stopped-process and late-historic reload decisions out of the status effect while leaving mutable refs, stream-controller closure, historic loading, and emitting in the hook.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationRunningStream.ts`, moving running stream subscription, initial baseline forwarding, stream id creation, stale snapshot suppression, empty running stream retry, active controller registration, finish emit callback, and error rejection out of the hook.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationHistoricBatches.ts`, moving historic process-state conversion, reverse chronological initial loading, remaining-process filtering, concurrency chunking, and batch continuation decisions out of the hook.
- Fix: added `frontend/src/hooks/useConversationHistory/conversationRemovalPlan.ts`, moving displayed-process removal detection and loading/error suppression out of the hook while leaving ref cleanup, controller closure, state deletion, and emitting in the hook.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationRuntimeStore.test.ts` for cloned cache writes, live reference writes, oldest-entry eviction, and stream id uniqueness/process-id inclusion; existing `useConversationHistory.test.ts` stayed green for multi-instance restore, stream reconnect baseline, loading reconnect preservation, delayed historic entries, late completed processes, stream cleanup, and assistant-prefix stripping.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationSnapshotStaleness.test.ts` for ignored user/token/loading entries, duplicate snapshot detection, self-snapshot suppression, and ordered sequence mismatches.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationTokenUsage.test.ts` for no-token state, newest-process priority, reverse scanning within a process, and fallback to older processes when newer ones have no token usage.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationEmitAddType.test.ts` for empty batches, non-plan final entries, and ExitPlanMode final entries across all add types.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationScriptDisplay.test.ts` for supported script context labels, dev-server suppression, running status, completed non-zero exit status, failed/killed flags, and the existing missing-live-process success fallback.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationContextCompactDisplay.test.ts` for non-compact prompts, running compact status, completed/missing compact status, failed/killed compact status, and the synthetic context compact patch key.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationCodingAgentDisplay.test.ts` for non-agent suppression, synthetic user prompt insertion, raw user/token filtering, assistant replay-prefix stripping, pending-approval loading suppression, running loading insertion, failed setup-required detection, and context compact delegation.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationHistoricEntriesLoader.test.ts` for normalized coding-agent historic loads, raw script historic loads, finished-event resolution, running snapshot idle resolution, and error fallback to latest entries.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationReloadPlan.test.ts` for displayed running-to-stopped reloads, non-displayed stopped suppression, late historic load planning after initial load, already-loading suppression, running suppression, and previous-status tracking.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationRunningStream.test.ts` for initial baseline forwarding, keyed streamed entry emission, stale snapshot suppression, empty running finish retry, active controller callbacks, and non-retryable error rejection/warning.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationHistoricBatches.test.ts` for patch-key conversion, initial newest-first chunking and threshold stop behavior, remaining-process filtering, and no-op remaining batches.
- Behavior lock: added `frontend/src/hooks/useConversationHistory/conversationRemovalPlan.test.ts` for loading/error suppression, removed-id detection, and no-op unchanged process sets.
- Verification: red test first failed on missing `conversationRuntimeStore`; `pnpm vitest run src/hooks/useConversationHistory/conversationRuntimeStore.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 2 files / 14 tests; first `frontend:check` caught an incomplete `ScriptRequest` test fixture, then `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed; `git diff --check` reported no whitespace errors.
- Verification (snapshot staleness pass): red test first failed on missing `conversationSnapshotStaleness`; `pnpm vitest run src/hooks/useConversationHistory/conversationSnapshotStaleness.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 3 files / 18 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (token usage pass): red test first failed on missing `conversationTokenUsage`; `pnpm vitest run src/hooks/useConversationHistory/conversationTokenUsage.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 4 files / 22 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed; `git diff --check` reported no whitespace errors.
- Verification (emit add-type pass): red test first failed on missing `conversationEmitAddType`; `pnpm vitest run src/hooks/useConversationHistory/conversationEmitAddType.test.ts` passed with 9 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 5 files / 31 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed; `git diff --check` reported no whitespace errors.
- Verification (script display pass): red test first failed on missing `conversationScriptDisplay`; first implementation test caught the existing `line.content` output coercion behavior, and first `frontend:check` caught missing `PatchType` union narrowing in the test; then `pnpm vitest run src/hooks/useConversationHistory/conversationScriptDisplay.test.ts` passed with 9 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 6 files / 40 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed; `git diff --check` reported no whitespace errors.
- Verification (context compact display pass): red test first failed on missing `conversationContextCompactDisplay`; first `frontend:check` caught incomplete executor-profile and follow-up request test fixtures, then `pnpm vitest run src/hooks/useConversationHistory/conversationContextCompactDisplay.test.ts` passed with 5 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 7 files / 45 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed; `git diff --check` reported no whitespace errors.
- Verification (coding-agent display pass): red test first failed on missing `conversationCodingAgentDisplay`; first `frontend:check` caught overly broad `PatchType` fixture content, wrong token usage fixture shape, missing pending-approval metadata, an insufficient setup-required type narrowing, and one stale hook import, then `pnpm vitest run src/hooks/useConversationHistory/conversationCodingAgentDisplay.test.ts` passed with 7 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 8 files / 52 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (historic loader pass): red test first failed on missing `conversationHistoricEntriesLoader`; `pnpm vitest run src/hooks/useConversationHistory/conversationHistoricEntriesLoader.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 9 files / 56 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (reload plan pass): red test first failed on missing `conversationReloadPlan`; `pnpm vitest run src/hooks/useConversationHistory/conversationReloadPlan.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 10 files / 60 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (running stream pass): red test first failed on missing `conversationRunningStream`; `pnpm vitest run src/hooks/useConversationHistory/conversationRunningStream.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 11 files / 64 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (historic batch pass): red test first failed on missing `conversationHistoricBatches`; first `frontend:check` caught the now-dead hook-local `patchWithKey`, then `pnpm vitest run src/hooks/useConversationHistory/conversationHistoricBatches.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 12 files / 68 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (removal plan pass): red test first failed on missing `conversationRemovalPlan`; `pnpm vitest run src/hooks/useConversationHistory/conversationRemovalPlan.test.ts` passed with 4 tests; `pnpm vitest run src/hooks/useConversationHistory/useConversationHistory.test.ts` passed with 10 tests; `pnpm vitest run src/hooks/useConversationHistory` passed with 13 files / 72 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Final review: `useConversationHistory.ts` now keeps React-owned lifecycle sequencing, refs, and emits; reusable policy and stream/display/batch/reload/removal logic lives behind tested helpers. The only remaining warning is the existing React `act(...)` warning in the delayed historic entries test, which still passes and is tracked as residual test-harness noise.

### FE-004: Kanban session surfaces leak orchestration and query boundaries

- Category: complex implementation, weak boundary, over-abstraction, missing tests
- Severity: high
- Confidence: high
- Files: `frontend/src/components/kanban/KanbanSessionHub.tsx`, `frontend/src/components/kanban/session-hub/SessionHubSidebar.tsx`, `frontend/src/components/kanban/KanbanSessionConversationView.tsx`
- Evidence: the hub owns creation, optimistic status updates, archive/delete flows, filters, resize, and navigation, then passes a wide prop bag into the sidebar; the conversation view also coupled session-detail query keys, enablement, and fetch input through a component-local `sessionId!` assertion.
- Risk: sidebar UI tests do not protect hub orchestration behavior, future state changes will spread across both components, and query enablement can drift from fetch input safety.
- Suggested behavior lock: orchestration tests for create/archive/delete/filter flows before moving state behind a smaller interface.
- Cleanup status: pass 1 extracted the session-detail query boundary; pass 2 extracted session list filtering/grouping/display-count derivation; pass 3 extracted bulk-delete result derivation; pass 4 extracted create-session submit and payload derivation; pass 5 extracted create/rename mutation side effects into a session-hub hook.
- Cleanup plans: [fe-004-session-query-cleanup-plan.md](fe-004-session-query-cleanup-plan.md), [fe-004-session-list-derivation-cleanup-plan.md](fe-004-session-list-derivation-cleanup-plan.md), [fe-004-bulk-delete-cleanup-plan.md](fe-004-bulk-delete-cleanup-plan.md), [fe-004-create-session-cleanup-plan.md](fe-004-create-session-cleanup-plan.md), [fe-004-mutation-hook-cleanup-plan.md](fe-004-mutation-hook-cleanup-plan.md).
- Fix: extracted `frontend/src/components/kanban/kanbanSessionConversationQuery.ts` so session-detail query key, enabled state, and fetch session id are pure and directly testable; `KanbanSessionConversationView.tsx` no longer uses `sessionId!` for session detail fetches.
- Fix: extended `frontend/src/components/kanban/session-hub/utils.ts` so executor filter options, workspace/executor filtering, status grouping, and displayed-count derivation are pure and directly testable instead of embedded in `KanbanSessionHub.tsx`.
- Fix: extended `frontend/src/components/kanban/session-hub/utils.ts` with `getBulkDeleteSessionSummary`, moving succeeded/failed delete result pairing, affected workspace derivation, failed selection restoration ids, and remaining session ids out of `KanbanSessionHub.tsx`.
- Fix: extended `frontend/src/components/kanban/session-hub/utils.ts` with `getCanCreateKanbanSession` and `getCreateProjectSessionRequest`, moving submit enablement, project/workspace validation, workspace/branch resolution, name trimming, and new-workspace repo payload derivation out of `KanbanSessionHub.tsx`.
- Fix: added `frontend/src/components/kanban/session-hub/useKanbanSessionMutations.ts`, moving create-session and rename-session mutation execution, scratch persistence, query invalidation, created-session placement, pending-id tracking, and create popover reset out of `KanbanSessionHub.tsx`.
- Behavior lock: added `frontend/src/components/kanban/kanbanSessionConversationQuery.test.ts` for missing-session and present-session query states; existing `KanbanSessionConversationView.test.tsx` stayed green for cached rendering, loading, missing-session, interactive shell, and placement behavior.
- Behavior lock: added `frontend/src/components/kanban/session-hub/utils.test.ts` for executor option sorting including the unassigned sentinel, workspace/executor filtering, status grouping, and displayed-count behavior.
- Behavior lock: expanded `frontend/src/components/kanban/session-hub/utils.test.ts` for mixed bulk delete success/failure, missing sessions, affected workspace dedupe, remaining session ids, and all-success failed-id suppression.
- Behavior lock: expanded `frontend/src/components/kanban/session-hub/utils.test.ts` for existing-workspace payloads, branch-only payloads, new-workspace repo payloads, create requirement errors, and submit enablement gates.
- Behavior lock: added `frontend/src/components/kanban/session-hub/UseKanbanSessionMutations.test.tsx` for create-session API payload execution, draft scratch persistence, create-success query invalidation, placement/pending/reset side effects, rename API execution, and rename query invalidation.
- Verification: `pnpm vitest run src/components/kanban/kanbanSessionConversationQuery.test.ts`; `pnpm vitest run src/components/kanban/KanbanSessionConversationView.test.tsx`; `pnpm vitest run src/components/kanban/session-hub/utils.test.ts`; `pnpm vitest run src/components/kanban/session-hub/SessionHubSidebar.test.tsx`; `pnpm vitest run src/components/kanban/session-hub/UseKanbanSessionMutations.test.tsx`; `pnpm vitest run src/components/kanban`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-005: File tree mixes derivation, IO, drag/drop, preview, and portal rendering

- Category: complex implementation, weak boundary, missing tests
- Severity: high
- Confidence: high
- Files: `frontend/src/components/file-tree/FileTreePanel.tsx`, `frontend/src/components/file-tree/FileTreePanel.truncated.test.tsx`
- Evidence: the file tree component owns lazy directory IO, path normalization, inline create/delete/copy, custom drag/drop, preview fetching, preview selection, and portal rendering. Existing direct coverage is narrow.
- Risk: file-tree to file-tree moves and file-tree to editor/composer drops can regress independently of render-only tests.
- Suggested behavior lock: desktop-runtime drag/drop cases plus unit coverage for path derivation and lazy-load behavior.
- Cleanup status: pass 1 extracted file-tree path resolution policy; pass 2 extracted lazy directory response normalization; pass 3 extracted preview selection snippet construction; pass 4 locked preview popover labels and fixed preview selection hint mojibake; pass 5 extracted merged entry and lazy-loadable directory derivation; pass 6 extracted folder git status aggregation; pass 7 extracted preview kind/status derivation; pass 8 extracted preview selection range derivation; pass 9 fixed duplicate-error mojibake and deleted stale native-menu dead code; pass 10 fixed create-file error label; pass 11 extracted new-item relative path derivation; pass 12 fixed delete error label; pass 13 extracted delete confirmation derivation; pass 14 extracted clipboard path text derivation; pass 15 extracted preview anchor derivation; pass 16 extracted context menu position derivation; pass 17 extracted context menu header derivation; pass 18 extracted keyboard action derivation; pass 19 extracted node view-state derivation; pass 20 extracted mention text derivation; pass 21 extracted folder expansion policy; pass 22 extracted preview insertion text derivation; pass 23 extracted new-item parent expansion policy; pass 24 extracted git status map derivation; pass 25 extracted preview image path derivation; pass 26 extracted inline new-item input config.
- Cleanup plans: [fe-005-path-resolution-cleanup-plan.md](fe-005-path-resolution-cleanup-plan.md), [fe-005-lazy-response-cleanup-plan.md](fe-005-lazy-response-cleanup-plan.md), [fe-005-preview-snippet-cleanup-plan.md](fe-005-preview-snippet-cleanup-plan.md), [fe-005-preview-labels-cleanup-plan.md](fe-005-preview-labels-cleanup-plan.md), [fe-005-derived-entries-cleanup-plan.md](fe-005-derived-entries-cleanup-plan.md), [fe-005-folder-git-status-cleanup-plan.md](fe-005-folder-git-status-cleanup-plan.md), [fe-005-preview-status-cleanup-plan.md](fe-005-preview-status-cleanup-plan.md), [fe-005-preview-selection-range-cleanup-plan.md](fe-005-preview-selection-range-cleanup-plan.md), [fe-005-duplicate-error-menu-dead-code-cleanup-plan.md](fe-005-duplicate-error-menu-dead-code-cleanup-plan.md), [fe-005-create-file-error-label-cleanup-plan.md](fe-005-create-file-error-label-cleanup-plan.md), [fe-005-new-item-path-cleanup-plan.md](fe-005-new-item-path-cleanup-plan.md), [fe-005-delete-error-label-cleanup-plan.md](fe-005-delete-error-label-cleanup-plan.md), [fe-005-delete-confirmation-cleanup-plan.md](fe-005-delete-confirmation-cleanup-plan.md), [fe-005-clipboard-path-cleanup-plan.md](fe-005-clipboard-path-cleanup-plan.md), [fe-005-preview-anchor-cleanup-plan.md](fe-005-preview-anchor-cleanup-plan.md), [fe-005-context-menu-position-cleanup-plan.md](fe-005-context-menu-position-cleanup-plan.md), [fe-005-context-menu-header-cleanup-plan.md](fe-005-context-menu-header-cleanup-plan.md), [fe-005-keyboard-action-cleanup-plan.md](fe-005-keyboard-action-cleanup-plan.md), [fe-005-node-view-state-cleanup-plan.md](fe-005-node-view-state-cleanup-plan.md), [fe-005-mention-text-cleanup-plan.md](fe-005-mention-text-cleanup-plan.md), [fe-005-folder-expansion-cleanup-plan.md](fe-005-folder-expansion-cleanup-plan.md), [fe-005-preview-insertion-cleanup-plan.md](fe-005-preview-insertion-cleanup-plan.md), [fe-005-new-item-parent-expansion-cleanup-plan.md](fe-005-new-item-parent-expansion-cleanup-plan.md), [fe-005-git-status-map-cleanup-plan.md](fe-005-git-status-map-cleanup-plan.md), [fe-005-preview-image-path-cleanup-plan.md](fe-005-preview-image-path-cleanup-plan.md), [fe-005-inline-new-input-cleanup-plan.md](fe-005-inline-new-input-cleanup-plan.md).
- Fix: added `resolveFileTreeAbsolutePath` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving Windows extended-prefix stripping, absolute-path detection, workspace separator selection, trailing-root trimming, and relative separator normalization out of `FileTreePanel.tsx`.
- Fix: added `normalizeDirectoryChildrenResponse` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving lazy directory response array guards out of the async component state mutation branch.
- Fix: added `buildFilePreviewSelectionSnippet` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving preview selected-line slicing, one-based range labels, language fence selection, and markdown snippet formatting out of `FileTreePanel.tsx`.
- Fix: added `getFilePreviewSelectionHints` in `frontend/src/components/file-tree/file-tree-utils.ts`, replacing the mojibake selection hints in `FileTreePanel.tsx` with tested readable Chinese labels.
- Fix: added `deriveFileTreeEntries` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving file/directory unioning, gitignored set unioning, special-directory lazy seeding, and discovered lazy-loadable merging out of adjacent component `useMemo` blocks.
- Fix: added `deriveFolderGitStatusMap` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving recursive folder git status aggregation and priority handling out of `FileTreePanel.tsx`.
- Fix: added `getFilePreviewKind` and `deriveFilePreviewDisplayState` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving text-vs-image preview kind and effective loading/error selection out of `FileTreePanel.tsx`.
- Fix: added `deriveFilePreviewSelectionRange` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving anchor/index ordering for click, shift-click, and drag selection out of `FileTreePanel.tsx`.
- Fix: fixed the duplicate-file failure toast to readable `创建副本失败` and deleted the unreachable commented native-menu implementation with stale mojibake labels.
- Fix: fixed the create-file failure toast to readable `创建文件失败`.
- Fix: added `buildNewFileTreeItemRelativePath` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving create-file/create-folder name trimming, fallback names, and optional parent joining out of duplicated callback code.
- Fix: fixed the delete failure toast to readable `删除失败`.
- Fix: added `buildFileTreeDeleteConfirmation` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving delete confirmation title, message, buttons, variant, and leaf-name extraction out of the trash callback.
- Fix: added `getFileTreeRelativeClipboardText` and `getFileTreeAbsoluteClipboardText` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving root relative/absolute clipboard fallback policy out of the async clipboard callbacks.
- Fix: added `deriveFilePreviewAnchor` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving preview popover width/height estimates, viewport clamping, and arrow offset calculation out of `FileTreePanel.tsx`.
- Fix: added `deriveFileTreeContextMenuPosition` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving context-menu viewport clamping and menu-size constants out of portal JSX.
- Fix: added `deriveFileTreeContextMenuHeader` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving root label fallback, relative-path subtitle, and leaf-title extraction out of portal JSX.
- Fix: added `deriveFileTreeKeyboardAction` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving selected-node delete/copy shortcut policy out of the `keydown` effect while leaving DOM filtering and dispatch in the component.
- Fix: added `deriveFileTreeNodeViewState` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving folder/file view flags, lazy state, git/ignored lookups, selected/drop flags, and row class assembly out of `renderNode`.
- Fix: added `getFileTreeMentionText` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving file-only trailing-space mention formatting out of the row action render branch.
- Fix: added `pruneExpandedFileTreeFolders`, `getAreAllVisibleFileTreeFoldersExpanded`, `toggleAllFileTreeFolders`, and `toggleFileTreeFolder` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving folder expansion policy and `Set` mutation copies out of `FileTreePanel.tsx`.
- Fix: added `getFilePreviewInsertionText` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving text-preview insertion eligibility and snippet creation out of `handleAddSelection`.
- Fix: added `ensureFileTreeParentFolderExpanded` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving parent-folder expansion policy out of duplicated new-file/new-folder prompt callbacks.
- Fix: added `deriveFileTreeGitStatusMap` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving git status array normalization out of `FileTreePanel.tsx`.
- Fix: added `getFilePreviewImagePath` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving image-preview path eligibility and absolute-path resolution out of `FileTreePanel.tsx`.
- Fix: added `getFileTreeInlineNewInputConfig` in `frontend/src/components/file-tree/file-tree-utils.ts`, moving inline new-item fallback name, icon path, and folder flag policy out of duplicated file/folder branches in `FileTreePanel.tsx`.
- Behavior lock: added `frontend/src/components/file-tree/file-tree-utils.test.ts` for POSIX joins, Windows joins, absolute POSIX/drive/UNC paths, and extended Windows path prefix handling.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for valid lazy response pass-through and missing/malformed response array normalization.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for single-line and multi-line preview snippet formatting, including known-language and unknown-language fences.
- Behavior lock: added `frontend/src/components/file-tree/FilePreviewPopover.test.tsx` for readable preview labels/actions, and expanded `file-tree-utils.test.ts` for readable text-preview selection hints.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for base/lazy entry dedupe, gitignored set merging, all-directory lazy loading, special-directory lazy loading, and discovered lazy-loadable directory preservation.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for descendant folder git status aggregation, priority ordering, and omitting folders without descendant statuses.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for text/image preview kind selection and effective preview loading/error normalization.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for ordered preview line-selection ranges in both anchor directions and single-line selections.
- Behavior lock: expanded `frontend/src/components/file-tree/FileTreePanel.truncated.test.tsx` for duplicate-file failure through the visible context menu and readable failure toast text.
- Behavior lock: expanded `frontend/src/components/file-tree/FileTreePanel.truncated.test.tsx` for root-level inline file creation failure and readable failure toast text.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for root-level and nested new-item relative paths, trimmed names, and fallback names.
- Behavior lock: expanded `frontend/src/components/file-tree/FileTreePanel.truncated.test.tsx` for context-menu delete confirmation, trash API path resolution, and readable delete failure toast text.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for file/folder delete confirmation copy and nested path leaf-name extraction.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for root/non-root relative clipboard text and root/non-root absolute clipboard text.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for normal and constrained viewport preview anchor positioning.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for context-menu cursor positioning, viewport-edge clamping, and small viewport lower bounds.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for root context menu labels and nested path leaf/subtitle derivation.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for Ctrl/Cmd Delete/Backspace delete actions, Ctrl/Cmd+C absolute-path copy actions, Shift+C suppression, missing selection suppression, and platform-specific primary modifier handling.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for lazy folder view state, loading/error state, folder git status lookup, ignored/selected/drop row classes, and regular file git/ignored classes.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for file mention trailing-space formatting and folder mention formatting.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for pruning stale expanded folders, all-visible-expanded detection, expand-all/collapse-all behavior, and single-folder toggling.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for text preview insertion text and null insertion results for image previews, missing paths, and missing selections.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for root no-op, already-expanded reference preservation, and collapsed-parent expansion without mutating the original set.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for missing git status input and duplicate-path last-write-wins mapping.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for null/text preview image-path suppression and relative/absolute image path resolution.
- Behavior lock: expanded `frontend/src/components/file-tree/file-tree-utils.test.ts` for file and folder inline new-input fallback names, icon paths, and folder flags.
- Verification (path resolution pass): red test first failed on missing `resolveFileTreeAbsolutePath`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 4 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 2 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (lazy response normalization pass): red test first failed on missing `normalizeDirectoryChildrenResponse`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 6 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 2 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (preview snippet pass): red test first failed on missing `buildFilePreviewSelectionSnippet`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 8 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 2 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (preview labels pass): `FilePreviewPopover` labels were already readable and the initial label test passed; `rg` then proved the real mojibake was in `FileTreePanel` selection hints. Red test first failed on missing `getFilePreviewSelectionHints`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts src/components/file-tree/FilePreviewPopover.test.tsx src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 13 tests; `rg` found no remaining targeted mojibake patterns in the file-tree preview files; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (derived entries pass): red test first failed on missing `deriveFileTreeEntries`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 11 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 2 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (folder git status pass): red test first failed on missing `deriveFolderGitStatusMap`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 12 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 2 tests; `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (preview status pass): red test first failed on missing `getFilePreviewKind` and `deriveFilePreviewDisplayState`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 15 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 2 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (preview selection range pass): red test first failed on missing `deriveFilePreviewSelectionRange`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 17 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 2 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (duplicate error/dead menu pass): red test first failed on unreadable duplicate failure toast; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 3 tests; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 17 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (create-file error label pass): red test first failed on English create-file failure toast; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 4 tests; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 17 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (new item path pass): red test first failed on missing `buildNewFileTreeItemRelativePath`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 19 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 4 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (delete error label pass): red test first failed on English delete failure toast; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 19 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (delete confirmation pass): red test first failed on missing `buildFileTreeDeleteConfirmation`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 20 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (clipboard path pass): red test first failed on missing clipboard text helpers; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 22 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (preview anchor pass): red test first failed on missing `deriveFilePreviewAnchor`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 24 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (context menu position pass): red test first failed on missing `deriveFileTreeContextMenuPosition`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 26 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (context menu header pass): red test first failed on missing `deriveFileTreeContextMenuHeader`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 28 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (keyboard action pass): red test first failed on missing `deriveFileTreeKeyboardAction`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 31 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (node view-state pass): red test first failed on missing `deriveFileTreeNodeViewState`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 33 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (mention text pass): red test first failed on missing `getFileTreeMentionText`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 34 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (folder expansion pass): red test first failed on missing folder expansion helpers; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 38 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (preview insertion pass): red test first failed on missing `getFilePreviewInsertionText`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 40 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (new-item parent expansion pass): red test first failed on missing `ensureFileTreeParentFolderExpanded`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 42 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (git status map pass): red test first failed on missing `deriveFileTreeGitStatusMap`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 44 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (preview image path pass): red test first failed on missing `getFilePreviewImagePath`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 46 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (inline new input pass): red test first failed on missing `getFileTreeInlineNewInputConfig`; `pnpm vitest run src/components/file-tree/file-tree-utils.test.ts` passed with 48 tests; `pnpm vitest run src/components/file-tree/FileTreePanel.truncated.test.tsx` passed with 5 tests; `pnpm run check` and `pnpm run lint` passed.

### FE-006: App root carries transition-state historical baggage

- Category: historical baggage, weak boundary
- Severity: medium-high
- Confidence: medium-high
- Files: `frontend/src/App.tsx`
- Evidence: app shell code mixes onboarding/maintenance side effects, global `legacy-design` body state, multiple shell entrypoints, and redirects disabled new UI routes back to legacy screens.
- Risk: routing, onboarding gates, and legacy/new design boundaries remain coupled.
- Suggested behavior lock: `MemoryRouter` tests for `/desktop-toast`, `/project-rail`, `/settings/*`, legacy redirects, and disclaimer/onboarding short-circuit behavior.
- Cleanup status: pass 1 extracted startup prompt gate ordering from the app effect; pass 2 extracted local dependency maintenance prompt policy; pass 3 extracted top-level route mode selection; pass 4 moved legacy body-class side effects into a focused hook; pass 5 extracted main-window close behavior storage/execution helpers; pass 6 extracted maintenance orchestration decisions; pass 7 extracted the main route table. FE-006 is completed for the identified App root boundary cleanup; remaining app-root code is provider, shell-entry, and effect orchestration.
- Cleanup plans: [fe-006-startup-prompt-gate-cleanup-plan.md](fe-006-startup-prompt-gate-cleanup-plan.md), [fe-006-maintenance-prompt-policy-cleanup-plan.md](fe-006-maintenance-prompt-policy-cleanup-plan.md), [fe-006-route-mode-cleanup-plan.md](fe-006-route-mode-cleanup-plan.md), [fe-006-legacy-body-class-cleanup-plan.md](fe-006-legacy-body-class-cleanup-plan.md), [fe-006-main-window-close-behavior-cleanup-plan.md](fe-006-main-window-close-behavior-cleanup-plan.md), [fe-006-maintenance-orchestration-cleanup-plan.md](fe-006-maintenance-orchestration-cleanup-plan.md), [fe-006-route-table-cleanup-plan.md](fe-006-route-table-cleanup-plan.md).
- Fix: added `frontend/src/appStartupPrompt.ts` so settings-route suppression, disclaimer priority, onboarding priority, and release-note dismissal are pure and directly tested instead of embedded in the `App.tsx` startup effect.
- Fix: added `frontend/src/appMaintenancePrompt.ts` so version-like comparison and local-tool update prompt decisions are pure and directly tested instead of embedded in the `App.tsx` maintenance effect.
- Fix: added `frontend/src/appRouteMode.ts` so `/desktop-toast`, `/project-rail`, and main-app shell selection is pure and directly tested instead of embedded as inline pathname comparisons in `AppContent`.
- Fix: added `frontend/src/useLegacyDesignBodyClass.ts` so `legacy-design` body class mutation is isolated from `MainAppContent`.
- Fix: added `frontend/src/mainWindowCloseBehavior.ts` so main-window close behavior storage parsing, persistence, and exit/minimize execution helpers are no longer embedded in `App.tsx`.
- Fix: added `frontend/src/appMaintenancePlan.ts` so maintenance start gating, app update toast eligibility, and local dependency prompt tool selection are pure and directly tested instead of embedded in the `App.tsx` maintenance effect.
- Fix: added `frontend/src/MainAppRoutes.tsx` so route declarations, legacy route scope, layout grouping, and legacy redirect paths are no longer embedded in `App.tsx`.
- Behavior lock: added `frontend/src/appStartupPrompt.test.ts` for settings suppression, disclaimer-first ordering, onboarding after disclaimer, release-note dismissal after onboarding, and no-op clear/missing-config states.
- Behavior lock: added `frontend/src/appMaintenancePrompt.test.ts` for version ordering, missing-tool prompting, unsupported installed versions, supported installed versions, and version-indeterminate installed tools.
- Behavior lock: added `frontend/src/appRouteMode.test.ts` for exact desktop-toast/project-rail shell selection, prefixed non-match routes, and ordinary main-app routes.
- Behavior lock: added `frontend/src/UseLegacyDesignBodyClass.test.tsx` for adding `legacy-design` while mounted and removing it on unmount.
- Behavior lock: added `frontend/src/mainWindowCloseBehavior.test.ts` for saved close behavior parsing, invalid-value suppression, and supported behavior persistence.
- Behavior lock: added `frontend/src/appMaintenancePlan.test.ts` for maintenance one-shot start gates, update-toast enablement, null-config suppression, and visible local-tool update prompt selection.
- Behavior lock: added `frontend/src/MainAppRoutes.test.tsx` for standard project routes, IDE workspace routes, full attempt logs layout isolation, settings index redirect, legacy MCP redirect, and disabled new-UI redirects.
- Verification: red test first failed on missing `appStartupPrompt`; `pnpm vitest run src/appStartupPrompt.test.ts` passed with 5 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (maintenance prompt policy pass): red test first failed on missing `appMaintenancePrompt`; `pnpm vitest run src/appMaintenancePrompt.test.ts` passed with 3 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (route mode pass): red test first failed on missing `appRouteMode`; `pnpm vitest run src/appRouteMode.test.ts` passed with 4 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (legacy body class pass): red test first failed on missing `useLegacyDesignBodyClass`; `pnpm vitest run src/UseLegacyDesignBodyClass.test.tsx` passed with 1 test; first `frontend:lint` caught the hook test filename casing, then `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (main-window close behavior pass): red test first failed on missing `mainWindowCloseBehavior`; `pnpm vitest run src/mainWindowCloseBehavior.test.ts` passed with 2 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (maintenance orchestration pass): red test first failed on missing `appMaintenancePlan`; `pnpm vitest run src/appMaintenancePlan.test.ts` passed with 3 tests; `pnpm vitest run src/appMaintenancePrompt.test.ts src/appMaintenancePlan.test.ts` passed with 6 tests; first `frontend:check` caught nullable config handling, then `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (route table pass): red test first failed on missing `MainAppRoutes`; `pnpm vitest run src/MainAppRoutes.test.tsx` passed with 6 tests; `pnpm vitest run src/MainAppRoutes.test.tsx src/appRouteMode.test.ts` passed with 10 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.

### FE-007: WYSIWYG wrapper aggregates plugin policy and app context

- Category: over-abstraction, complex implementation, missing tests
- Severity: medium-high
- Confidence: high
- Files: `frontend/src/components/ui/wysiwyg.tsx`
- Evidence: one wrapper owns Lexical theme config, transformer selection, drag-and-drop intake, task/session context wiring, typeahead plugins, keyboard commands, and read-only actions.
- Risk: plugin-level tests do not prove wrapper behavior or app-context integration.
- Suggested behavior lock: wrapper tests for read-only mode, typeahead enablement, drag/drop intake, and task/session context interactions.
- Cleanup status: pass 1 extracted markdown preset transformer policy; pass 2 extracted read-only action presentation; pass 3 extracted editing plugin selection policy; pass 4 extracted file-reference drag/drop intake policy; pass 5 extracted Lexical editor config policy; pass 6 extracted read-only plugin grouping; pass 7 extracted editable plugin grouping; pass 8 extracted editor context provider composition; pass 9 extracted file-reference insertion mutation; pass 10 extracted file-reference drop runtime handlers; pass 11 extracted the editor shell composition. FE-007 is completed for the identified WYSIWYG wrapper boundary; the remaining wrapper now owns prop/state orchestration, copy actions, and read-only action wrapping.
- Cleanup plans: [fe-007-transformer-policy-cleanup-plan.md](fe-007-transformer-policy-cleanup-plan.md), [fe-007-read-only-actions-cleanup-plan.md](fe-007-read-only-actions-cleanup-plan.md), [fe-007-editing-plugin-policy-cleanup-plan.md](fe-007-editing-plugin-policy-cleanup-plan.md), [fe-007-file-reference-drop-policy-cleanup-plan.md](fe-007-file-reference-drop-policy-cleanup-plan.md), [fe-007-editor-config-cleanup-plan.md](fe-007-editor-config-cleanup-plan.md), [fe-007-read-only-plugins-cleanup-plan.md](fe-007-read-only-plugins-cleanup-plan.md), [fe-007-editable-plugins-cleanup-plan.md](fe-007-editable-plugins-cleanup-plan.md), [fe-007-context-providers-cleanup-plan.md](fe-007-context-providers-cleanup-plan.md), [fe-007-file-reference-insertion-cleanup-plan.md](fe-007-file-reference-insertion-cleanup-plan.md), [fe-007-file-reference-drop-runtime-cleanup-plan.md](fe-007-file-reference-drop-runtime-cleanup-plan.md), [fe-007-editor-shell-cleanup-plan.md](fe-007-editor-shell-cleanup-plan.md).
- Fix: added `frontend/src/components/ui/wysiwyg/wysiwyg-markdown-policy.ts` so default vs session-input-minimal active transformers and shortcut transformers are pure and directly tested instead of embedded in `WYSIWYGEditor`.
- Fix: added `frontend/src/components/ui/wysiwyg/read-only-actions.tsx` so copy/edit/delete presentation and callback wiring are no longer embedded beside Lexical editor setup.
- Fix: added `frontend/src/components/ui/wysiwyg/editing-plugin-policy.ts` so editable plugin enablement for presets, executor profiles, autofocus, and clicked-element insertion is pure and directly tested instead of embedded in plugin JSX conditions.
- Fix: added `frontend/src/components/ui/wysiwyg/file-reference-drop-policy.ts` so MIME detection, serialized-vs-current payload selection, disabled suppression, and custom drop payload intake are pure and directly tested instead of embedded in drag/drop handlers.
- Fix: added `frontend/src/components/ui/wysiwyg/editor-config-policy.ts` so Lexical namespace, error handling, theme classes, code-highlight theme, and node registration are pure and directly tested instead of embedded in `WYSIWYGEditor`.
- Fix: added `frontend/src/components/ui/wysiwyg/read-only-plugins.tsx` so read-only link sanitization and conditional clickable-code plugin wiring are grouped outside the main wrapper.
- Fix: added `frontend/src/components/ui/wysiwyg/editable-plugins.tsx` so editable-mode plugin rendering, typeahead provider grouping, command plugin gates, keyboard command props, and clicked-element insertion wiring are grouped outside the main wrapper.
- Fix: added `frontend/src/components/ui/wysiwyg/editor-context-providers.tsx` so task attempt, task, and local image contexts are composed outside the main wrapper.
- Fix: added `frontend/src/components/ui/wysiwyg/file-reference-insertion.ts` so the Lexical mutation for inserting file-reference chips is tested outside the main wrapper.
- Fix: added `frontend/src/components/ui/wysiwyg/use-file-reference-drop-handlers.ts` so dragover/drop/custom-drop runtime wiring, current-drag fallback, insertion dispatch, and drag-state clearing are outside the main wrapper.
- Fix: added `frontend/src/components/ui/wysiwyg/editor-shell.tsx` so Lexical composer setup, markdown sync wiring, toolbars, content editable drag wiring, core plugins, and editable/read-only plugin-group switching are outside the main wrapper.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts` for default transformer order, default shortcut heading suppression, minimal transformer scope, and minimal shortcut suppression.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/read-only-actions.test.tsx` for copy label state, copy click, conditional edit/delete rendering, and edit/delete callbacks.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/editing-plugin-policy.test.ts` for disabled-mode suppression, default/minimal preset plugin policy, slash and dollar command gating, autofocus, and clicked-element insert gating.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/file-reference-drop-policy.test.ts` for disabled drag/drop rejection, MIME/current-payload dragover acceptance, serialized drop priority, current-payload fallback, unrelated drop suppression, and custom drop detail handling.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/editor-config-policy.test.ts` for default editor identity/error handler/theme/node order and session-input-minimal compact theme behavior.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/read-only-plugins.test.tsx` for always-on link sanitization, clickable-code callback gating, and callback passthrough.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/editable-plugins.test.tsx` for full editable plugin rendering, policy-disabled suppression, executor-profile gating, clicked-element callback gating, and keyboard command prop passthrough.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/editor-context-providers.test.tsx` for task attempt id, task id, local image propagation, and empty local image default behavior.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/file-reference-insertion.test.ts` for missing payload/editor no-op, range selection insertion, append-to-last-element fallback, and create-paragraph fallback.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/use-file-reference-drop-handlers.test.tsx` for accepted dragover copy behavior, disabled suppression, serialized drop insertion/clear, custom drop insertion/clear, and custom listener cleanup.
- Behavior lock: added `frontend/src/components/ui/wysiwyg/editor-shell.test.tsx` for context provider props, composer config, markdown sync props, toolbar gating, core plugin rendering, editable/read-only plugin switching, content editable labeling/classes, refs, and drag handler wiring.
- Verification (transformer policy pass): red test first failed on missing `wysiwygMarkdownPolicy`; `pnpm vitest run src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/plugins/typeahead-triggers.test.ts` passed with 20 tests; first `frontend:lint` caught non-kebab-case filenames, then `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (read-only actions pass): red test first failed on missing `read-only-actions`; `pnpm vitest run src/components/ui/wysiwyg/read-only-actions.test.tsx src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts` passed with 8 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (editing plugin policy pass): red test first failed on missing `editing-plugin-policy`; `pnpm vitest run src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 14 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (file-reference drop policy pass): red test first failed on missing `file-reference-drop-policy`; `pnpm vitest run src/components/ui/wysiwyg/file-reference-drop-policy.test.ts src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 20 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.
- Verification (editor config policy pass): red test first failed on missing `editor-config-policy`; `pnpm vitest run src/components/ui/wysiwyg/editor-config-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/file-reference-drop-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 22 tests; `pnpm run frontend:check`, `pnpm run frontend:lint`, `pnpm run check`, and `pnpm run lint` passed.
- Verification (read-only plugin grouping pass): red test first failed on missing `read-only-plugins`; `pnpm vitest run src/components/ui/wysiwyg/read-only-plugins.test.tsx src/components/ui/wysiwyg/editor-config-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/file-reference-drop-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 25 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (editable plugin grouping pass): red test first failed on missing `editable-plugins`; `pnpm vitest run src/components/ui/wysiwyg/editable-plugins.test.tsx src/components/ui/wysiwyg/read-only-plugins.test.tsx src/components/ui/wysiwyg/editor-config-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/file-reference-drop-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 28 tests; first `pnpm run check` caught an invalid generated-type enum fixture, then `pnpm run check` and `pnpm run lint` passed.
- Verification (context provider pass): red test first failed on missing `editor-context-providers`; `pnpm vitest run src/components/ui/wysiwyg/editor-context-providers.test.tsx src/components/ui/wysiwyg/editable-plugins.test.tsx src/components/ui/wysiwyg/read-only-plugins.test.tsx src/components/ui/wysiwyg/editor-config-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/file-reference-drop-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 30 tests; `pnpm run check` and `pnpm run lint` passed.
- Verification (file-reference insertion pass): red test first failed on missing `file-reference-insertion`; `pnpm vitest run src/components/ui/wysiwyg/file-reference-insertion.test.ts src/components/ui/wysiwyg/editor-context-providers.test.tsx src/components/ui/wysiwyg/editable-plugins.test.tsx src/components/ui/wysiwyg/read-only-plugins.test.tsx src/components/ui/wysiwyg/editor-config-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/file-reference-drop-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 34 tests; first `pnpm run check` caught a too-narrow mock editor fixture, then `pnpm run check` and `pnpm run lint` passed.
- Verification (file-reference drop runtime pass): red test first failed on missing `use-file-reference-drop-handlers`; `pnpm vitest run src/components/ui/wysiwyg/use-file-reference-drop-handlers.test.tsx src/components/ui/wysiwyg/file-reference-insertion.test.ts src/components/ui/wysiwyg/editor-context-providers.test.tsx src/components/ui/wysiwyg/editable-plugins.test.tsx src/components/ui/wysiwyg/read-only-plugins.test.tsx src/components/ui/wysiwyg/editor-config-policy.test.ts src/components/ui/wysiwyg/wysiwyg-markdown-policy.test.ts src/components/ui/wysiwyg/editing-plugin-policy.test.ts src/components/ui/wysiwyg/file-reference-drop-policy.test.ts src/components/ui/wysiwyg/read-only-actions.test.tsx` passed with 39 tests; first `pnpm run check` caught the still-needed `useEffect` import in `EditorRefPlugin`, then `pnpm run check` and `pnpm run lint` passed.
- Verification (editor shell pass): red test first failed on missing `editor-shell`; `pnpm vitest run src/components/ui/wysiwyg/editor-shell.test.tsx` passed with 3 tests; FE-007 regression group passed with 11 files / 42 tests; first `pnpm run check` caught a stale unused test import, an incomplete Lexical config fixture, and too-wide placeholder typing, then `pnpm run check`, `pnpm run lint`, and `git diff --check` passed.

### FE-008: Project card menu labels contain user-visible mojibake

- Category: historical baggage, user-facing text corruption, missing tests
- Severity: medium
- Confidence: high
- Files: `frontend/src/components/projects/ProjectCard.tsx`
- Evidence: project-card menu items and created-date metadata rendered corrupted strings for details, open in IDE, edit, delete, and creation date.
- Risk: project navigation and management actions are visible but unreadable in the project list.
- Cleanup status: fixed in this pass.
- Plan: [fe-008-project-card-labels-cleanup-plan.md](fe-008-project-card-labels-cleanup-plan.md) documents the bounded label cleanup and presentation-only test.
- Fix: replaced corrupted labels with readable Chinese copy: `查看详情`, `在 IDE 中打开`, `编辑`, `删除`, and `创建于`.
- Behavior lock: added `frontend/src/components/projects/ProjectCard.test.tsx` covering readable card metadata and dropdown labels for a single-repo project.
- Verification: red `pnpm vitest run src/components/projects/ProjectCard.test.tsx` first failed on the corrupted created-date label; after the fix and realistic Radix menu interaction, the test passed. Full verification: `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-009: Shared `cn()` helper disables Tailwind conflict merging

- Category: historical baggage, dead code, weak UI utility boundary
- Severity: medium
- Confidence: high
- Files: `frontend/src/lib/utils.ts`, `frontend/src/lib/utils.test.ts`
- Evidence: `cn()` carried a commented-out `tailwind-merge` import and stale TODO while `tailwind-merge` is already installed and Tailwind 3.4 is the active project version.
- Risk: shared UI component call sites can emit contradictory utility classes such as `p-2 p-4`, making final styling depend on accidental class order outside the helper.
- Cleanup status: fixed in this pass.
- Plan: [fe-009-cn-tailwind-merge-cleanup-plan.md](fe-009-cn-tailwind-merge-cleanup-plan.md) documents the bounded helper cleanup and custom-token preservation checks.
- Fix: restored `twMerge(clsx(inputs))` and removed the disabled import/TODO block.
- Behavior lock: extended `frontend/src/lib/utils.test.ts` to prove conflicting Tailwind utilities collapse to the later class and custom text color tokens still coexist with text-size utilities.
- Verification: red `pnpm vitest run src/lib/utils.test.ts` first failed because `cn('p-2', 'p-4')` returned `p-2 p-4`; after the fix, the focused test passed. Full verification: `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-010: Frontend maintenance comments contain mojibake

- Category: historical baggage, readability
- Severity: low
- Confidence: high
- Files: `frontend/src/hooks/useLogStream.ts`, `frontend/src/stores/useTerminalStore.ts`
- Evidence: log-stream and terminal-session comments contained corrupted punctuation around `Finished` handling and `ExecutionProcess ID` metadata.
- Risk: maintainers reading log stream semantics or log-viewer terminal metadata see corrupted guidance in otherwise simple ownership comments.
- Cleanup status: fixed in this pass.
- Plan: [fe-010-frontend-comment-mojibake-cleanup-plan.md](fe-010-frontend-comment-mojibake-cleanup-plan.md) documents the comment-only cleanup scope.
- Fix: replaced corrupted punctuation with readable ASCII comments without changing runtime code or types.
- Behavior lock: targeted mojibake grep before/after plus TypeScript/lint/full-gate verification.
- Verification: `rg` found the two corrupted comments before the edit and no targeted frontend mojibake after the edit; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-011: Commit graph presentation copy and time formatting are embedded in the component

- Category: weak boundary, missing tests, readability
- Severity: low
- Confidence: high
- Files: `frontend/src/components/git/CommitGraph.tsx`, `frontend/src/components/git/commitGraphPresentation.ts`
- Evidence: `CommitGraph.tsx` owned relative-time formatting and user-facing graph labels directly in the render module, with no focused tests for seconds/minutes/hours/days/date fallback or readable labels.
- Risk: commit graph presentation copy can regress during graph/SVG/query edits, and the relative-time cases are hard to validate without rendering the full component.
- Cleanup status: fixed in this pass.
- Plan: [fe-011-commit-graph-label-cleanup-plan.md](fe-011-commit-graph-label-cleanup-plan.md) documents the bounded presentation extraction.
- Fix: extracted `COMMIT_GRAPH_LABELS` and `formatCommitTimeAgo` into `commitGraphPresentation.ts`; `CommitGraph.tsx` now consumes the tested helper without changing query, lane, click, or SVG behavior.
- Behavior lock: added `frontend/src/components/git/commitGraphPresentation.test.ts` for readable labels, seconds/minutes/hours/days formatting, and older-date fallback.
- Verification: red `pnpm vitest run src/components/git/commitGraphPresentation.test.ts` first failed on the missing helper; after extraction, the focused test passed. Full verification: `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-012: Image API exposes an unused empty URL placeholder

- Category: dead code, stale TODO, weak API boundary
- Severity: low
- Confidence: high
- Files: `frontend/src/lib/api/misc.ts`
- Evidence: `imagesApi.getImageUrl` always returned an empty string and carried a Tauri image-serving TODO, while `rg -n "getImageUrl\(" frontend/src` found no callers.
- Risk: future image preview work could accidentally consume a placeholder API that silently returns an invalid URL instead of using the existing upload metadata, blob preview, or file-tree image preview path.
- Cleanup status: fixed in this pass.
- Plan: [fe-012-image-url-placeholder-cleanup-plan.md](fe-012-image-url-placeholder-cleanup-plan.md) documents the dead placeholder cleanup.
- Fix: deleted the unused `getImageUrl` property and stale TODO; upload, task upload, workspace upload, delete, and list image APIs are unchanged.
- Behavior lock: reference search proved no frontend caller used the placeholder; frontend/repo typecheck and lint prove no internal API consumer depended on the property.
- Verification: `rg -n "getImageUrl\(" frontend/src`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-013: UI preferences scratch conversion is embedded in the hook

- Category: weak persistence boundary, missing tests
- Severity: medium
- Confidence: high
- Files: `frontend/src/hooks/useUiPreferencesScratch.ts`, `frontend/src/hooks/uiPreferencesScratchData.ts`, `frontend/src/hooks/uiPreferencesScratchData.test.ts`
- Evidence: `useUiPreferencesScratch.ts` owned store-to-scratch serialization, scratch-to-store hydration, defaulting, and legacy `file_search_repo_by_project` fallback inline with React effect/debounce state.
- Risk: persistence payload changes could alter hydration defaults or legacy repo fallback while reviewers are looking at hook timing instead of data conversion semantics.
- Cleanup status: fixed in this pass.
- Plan: [fe-013-ui-preferences-scratch-data-cleanup-plan.md](fe-013-ui-preferences-scratch-data-cleanup-plan.md) documents the pure conversion extraction and behavior locks.
- Fix: extracted `uiPreferencesStoreToScratchData` and `uiPreferencesScratchDataToStore` into a tested helper module; the hook now only coordinates scratch IO, Zustand state application, and debounced saving.
- Behavior lock: red `pnpm vitest run src/hooks/uiPreferencesScratchData.test.ts` first failed on the missing helper module; after extraction, tests cover snake_case serialization, hydration defaults, workspace panel state mapping, and current-vs-legacy file-search repo fallback.
- Verification: `pnpm vitest run src/hooks/uiPreferencesScratchData.test.ts`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-014: MCP strategy repeats JSON path traversal per operation

- Category: duplication, weak config boundary, missing tests
- Severity: medium
- Confidence: high
- Files: `frontend/src/lib/mcpStrategies.ts`, `frontend/src/lib/mcpStrategies.test.ts`
- Evidence: create, validate, extract, and preconfigured-server insertion each cloned JSON and traversed `servers_path` independently.
- Risk: frontend MCP config editing can drift between full-config creation, validation, API extraction, and preconfigured insertion, especially around missing paths, non-object intermediate values, and empty-path semantics.
- Cleanup status: fixed in this pass.
- Plan: [fe-014-mcp-strategy-path-cleanup-plan.md](fe-014-mcp-strategy-path-cleanup-plan.md) documents the path traversal cleanup while preserving current empty-path frontend semantics.
- Fix: extracted shared JSON object cloning, path label, path read, and object-path creation helpers; all four public strategy operations now use the same traversal primitives.
- Behavior lock: pre-refactor `pnpm vitest run src/lib/mcpStrategies.test.ts` passed, proving nested config creation, missing-path errors, non-object server rejection, and empty-path root preconfigured insertion before the cleanup; the same tests passed after extraction.
- Verification: `pnpm vitest run src/lib/mcpStrategies.test.ts`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-015: Created-at timestamp sorting repeats runtime type casts

- Category: duplication, weak generated-type/runtime boundary, missing tests
- Severity: medium
- Confidence: high
- Files: `frontend/src/utils/date.ts`, `frontend/src/utils/date.test.ts`, `frontend/src/components/dialogs/scripts/ScriptFixerDialog.tsx`, `frontend/src/hooks/useDevserverPreview.ts`, `frontend/src/hooks/useExecutionProcesses.ts`, `frontend/src/hooks/useProjects.ts`, `frontend/src/hooks/useConversationHistory/useConversationHistory.ts`, `frontend/src/hooks/useConversationHistory/conversationTokenUsage.ts`
- Evidence: multiple frontend sort paths converted backend timestamps with `new Date(value as unknown as string).getTime()`, duplicating the same runtime JSON boundary workaround while generated types expose a mix of `Date` and `string`.
- Risk: future timestamp sorting can drift between `Date`-typed projects and string-typed execution processes, or reintroduce unsafe local casts instead of one tested boundary helper.
- Cleanup status: fixed in this pass.
- Plan: [fe-015-date-timestamp-boundary-cleanup-plan.md](fe-015-date-timestamp-boundary-cleanup-plan.md) documents the shared timestamp helper extraction and sort-direction preservation.
- Fix: added `dateTimestamp` as the shared `Date`/string timestamp boundary and routed created-at sorting through it in project, execution-process, dev-server, script-fixer, conversation history, and token-usage paths.
- Behavior lock: red `pnpm vitest run src/utils/date.test.ts` first failed on missing `dateTimestamp`; after implementation, tests cover ISO strings, `Date` objects, and invalid-string `NaN` behavior.
- Verification: `pnpm vitest run src/utils/date.test.ts`; `rg -n "created_at as unknown as string|new Date\\([^\\n]*(as unknown as string)" frontend/src`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-016: Timestamp comparisons still parse dates at call sites

- Category: duplication, weak generated-type/runtime boundary
- Severity: medium
- Confidence: high
- Files: `frontend/src/utils/date.ts`, `frontend/src/utils/date.test.ts`, `frontend/src/lib/workspaceBranchOptions.ts`, `frontend/src/lib/devServerUtils.ts`, `frontend/src/hooks/git/useGitBranches.ts`, `frontend/src/hooks/useProjectWorkspacesStream.ts`, `frontend/src/hooks/useProjectWorktrees.ts`, `frontend/src/hooks/useKanbanProjectSessions.ts`, `frontend/src/components/kanban/session-hub/utils.ts`, `frontend/src/components/layout/ProjectWindowManager.tsx`, `frontend/src/components/panels/DockviewKanbanPanel.tsx`, `frontend/src/components/NormalizedConversation/PendingApprovalEntry.tsx`, `frontend/src/contexts/ApprovalFeedbackContext.tsx`
- Evidence: after FE-015, non-display comparisons for `updated_at`, `updatedAt`, `started_at`, `last_commit_date`, and approval timeout values still parsed timestamps locally with `new Date(...).getTime()` or direct `Date` comparisons.
- Risk: timestamp comparison semantics can drift between stream data, summaries, dev-server dedupe, branch sorting, and approval timeout paths.
- Cleanup status: fixed in this pass.
- Plan: [fe-016-timestamp-comparison-boundary-cleanup-plan.md](fe-016-timestamp-comparison-boundary-cleanup-plan.md) documents the non-display timestamp comparison cleanup.
- Fix: routed non-display timestamp comparisons through `dateTimestamp`; widened the helper to accept numeric millisecond timestamps for approval countdown inputs while preserving existing `Date` and string behavior.
- Behavior lock: `pnpm vitest run src/utils/date.test.ts` covers ISO strings, `Date` objects, numeric millisecond timestamps, and invalid-string `NaN` behavior; existing workspace-branch and Kanban session utility tests stayed green after rerouting comparisons.
- Verification: `pnpm vitest run src/utils/date.test.ts src/lib/workspaceBranchOptions.test.ts src/components/kanban/session-hub/utils.test.ts`; `rg -n 'new Date\\([^\\n]+\\)\\.getTime\\(\\)|new Date\\([^\\n]+\\) > new Date|new Date\\([^\\n]+\\) < new Date' frontend/src --glob '!**/*.test.*' --glob '!**/*.stories.*'`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-017: Unused file type icon module duplicates the real file-icon path

- Category: dead code, weak type boundary
- Severity: low
- Confidence: high
- Files: `frontend/src/utils/fileTypeIcon.ts`, `frontend/src/utils/fileIcons.ts`, `frontend/src/components/FileIcon.tsx`
- Evidence: `frontend/src/utils/fileTypeIcon.ts` defined an unused Lucide-based file icon mapping with an `as unknown as` icon adapter, while `rg -n "fileTypeIcon|@/utils/fileTypeIcon|../utils/fileTypeIcon|utils/fileTypeIcon" frontend/src` found no imports. The active file icon path is `frontend/src/utils/fileIcons.ts`, consumed by `frontend/src/components/FileIcon.tsx`.
- Risk: future file-tree work could modify or trust the dead module instead of the SVG icon path actually used by the app.
- Cleanup status: fixed in this pass.
- Plan: [fe-017-unused-file-type-icon-module-cleanup-plan.md](fe-017-unused-file-type-icon-module-cleanup-plan.md) documents the dead module deletion.
- Fix: deleted `frontend/src/utils/fileTypeIcon.ts`; no active imports or callers were removed.
- Behavior lock: pre-delete and post-delete import searches found no `fileTypeIcon` consumers; post-delete `rg -n "getFileIcon\\(" frontend/src` shows the active file icon function remains in `fileIcons.ts` and `FileIcon.tsx`.
- Verification: `rg -n "fileTypeIcon|@/utils/fileTypeIcon|../utils/fileTypeIcon|utils/fileTypeIcon" frontend/src`; `rg -n "getFileIcon\\(" frontend/src`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-018: WYSIWYG decorator-node factory drops its type predicate

- Category: weak type boundary, duplication, missing tests
- Severity: low
- Confidence: high
- Files: `frontend/src/components/ui/wysiwyg/lib/create-decorator-node.tsx`, `frontend/src/components/ui/wysiwyg/lib/create-decorator-node.test.tsx`
- Evidence: transformer helpers accepted `isNode` as a boolean function, then repeated `(node as unknown as { getData(): T }).getData()` in inline and fenced export branches even though the caller already provides a type guard.
- Risk: future generated decorator-node changes could bypass the factory's real node contract or duplicate more casts instead of using the narrowed node type.
- Cleanup status: fixed in this pass.
- Plan: [fe-018-wysiwyg-decorator-node-type-boundary-cleanup-plan.md](fe-018-wysiwyg-decorator-node-type-boundary-cleanup-plan.md) documents the transformer type-boundary cleanup.
- Fix: preserved the `isNode` type predicate through transformer helper signatures and replaced repeated `unknown` casts with direct `node.getData()` after narrowing.
- Behavior lock: added `create-decorator-node.test.tsx`; first test fixture failed outside Lexical's editor boundary, then the corrected fixture locked inline export, non-generated-node null export, and fenced export behavior inside a real `createEditor().update` boundary.
- Verification: `pnpm vitest run src/components/ui/wysiwyg/lib/create-decorator-node.test.tsx`; `rg -n "as unknown as \\{ getData\\(\\): T \\}" frontend/src/components/ui/wysiwyg/lib/create-decorator-node.tsx`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-019: Terminal tab close keyboard path casts events across domains

- Category: weak UI event boundary, missing tests
- Severity: low
- Confidence: high
- Files: `frontend/src/components/panels/DockviewTerminalPanel.tsx`, `frontend/src/components/panels/terminalTabClosePolicy.ts`, `frontend/src/components/panels/DockviewTerminalPanel.test.ts`
- Evidence: the terminal tab close keyboard handler reused the mouse close handler by casting `KeyboardEvent` to `React.MouseEvent`, even though the handler only needs `stopPropagation`; Enter/Space close-key policy was embedded inline.
- Risk: future terminal tab changes can accidentally rely on mouse-only event fields from the keyboard path or change keyboard close activation without tests.
- Cleanup status: fixed in this pass.
- Plan: [fe-019-terminal-tab-close-event-boundary-cleanup-plan.md](fe-019-terminal-tab-close-event-boundary-cleanup-plan.md) documents the event-boundary cleanup.
- Fix: extracted `isTerminalTabCloseKey`, tested Enter/Space activation, typed the close handler to the event capability it actually uses, and removed the keyboard-to-mouse cast.
- Behavior lock: red `pnpm vitest run src/components/panels/DockviewTerminalPanel.test.ts` first failed on missing policy; after extracting the pure policy module, the test covers Enter, Space, Escape, and literal `Space`.
- Verification: `pnpm vitest run src/components/panels/DockviewTerminalPanel.test.ts`; `rg -n "event as unknown as React.MouseEvent|isTerminalTabCloseKey" frontend/src/components/panels`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### FE-020: Dockview group element access hides a runtime capability behind an unknown cast

- Category: weak UI runtime boundary, missing test fidelity
- Severity: low
- Confidence: high
- Files: `frontend/src/utils/dockviewGroupPolicy.ts`, `frontend/src/utils/dockviewGroupPolicy.test.ts`
- Evidence: `getGroupElement` read Dockview's runtime `element` property through `group as unknown as { element?: HTMLElement }`, while the local group boundary type already owns the rest of the policy shape used by editor placement and header hiding.
- Risk: future Dockview group-placement changes can keep adding casts instead of documenting the exact runtime capability the policy needs; tests can also pass with non-DOM fixtures that no longer match the typed boundary.
- Cleanup status: fixed in this pass.
- Plan: [fe-020-dockview-group-element-boundary-cleanup-plan.md](fe-020-dockview-group-element-boundary-cleanup-plan.md) documents the bounded type-boundary cleanup.
- Fix: added optional `element?: HTMLElement` to the local `DockviewGroupLike` boundary, replaced the `unknown` cast with direct optional access, and changed the DOM-rect test fixture to use a real `HTMLElement`.
- Behavior lock: pre-edit `pnpm vitest run src/utils/dockviewGroupPolicy.test.ts` passed with coverage for group classification, welcome placeholder split exclusion, DOM-position editor-group ordering, id fallback, and next editor-group id selection; the same focused test passed after the cleanup.
- Verification: `pnpm vitest run src/utils/dockviewGroupPolicy.test.ts`; `rg -n "group as unknown as \\{ element\\?: HTMLElement \\}|as unknown as" frontend/src/utils/dockviewGroupPolicy.ts`; `pnpm run frontend:check`; `pnpm run frontend:lint`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

## Safest Cleanup Candidates

1. FE-005 file tree boundary cleanup. Path resolution, lazy response normalization, preview snippet construction, preview selection hints, merged entry derivation, folder git status aggregation, preview status derivation, preview selection range derivation, new-item path construction, delete confirmation construction, and clipboard path text derivation have pure helpers now; duplicate/create-file/delete error labels are readable and dead native-menu code is removed. Remaining preview interaction policy is browser-testable, while drag/drop still needs desktop-app verification.
2. FE-005 desktop drag/drop verification plan. The code can be reviewed now, but behavioral changes should wait for Tauri/WebView evidence.
3. Fresh FE-002 internals review. The previously identified parent/actionbar/input prop-surface cleanup is closed; further work should start from a new review of `SessionComposerInput` internals rather than assuming the old checklist still applies.

## Uncertainties

- FE-005 needs desktop-app verification for drag/drop changes. Browser-only tests are insufficient for this repo's Dockview/Tauri/WebView environment.

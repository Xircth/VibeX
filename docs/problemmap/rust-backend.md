# ProblemMap: Rust Backend And Runtime

Scope: `crates/**`, excluding `target`, vendored code, and frontend code. Findings are intentionally not downgraded for size or effort.

## Problems

### RB-001: Container runtime abstraction is a god trait with one real implementation

- Category: over-abstraction, complex implementation, weak boundary, missing tests
- Severity: high
- Confidence: high
- Files: `crates/services/src/services/container.rs`, `crates/local-deployment/src/container.rs`
- Evidence: `ContainerService` owns substantial workflow logic such as finalization, reset, stop, and action scheduling, while the inspected repo has one real implementor: `LocalContainerService`.
- Risk: the trait looks like a portability boundary but carries orchestration logic itself, making behavior harder to test or replace.
- Suggested behavior lock: service-level tests around finalize/reset/stop/start-next transitions before extracting pure workflow helpers or collapsing false abstraction.
- Cleanup status: pass 34 completed.
- Plan: [rb-001-container-cleanup-plan.md](rb-001-container-cleanup-plan.md) documents the constrained first slice and explicitly defers trait deletion or async workflow migration until higher-level transitions are behavior-locked.
- Plan: [rb-001-finalize-notification-policy-plan.md](rb-001-finalize-notification-policy-plan.md) documents the finalize notification policy extraction while leaving DB updates and notification delivery in `finalize_task`.
- Plan: [rb-001-reset-target-policy-plan.md](rb-001-reset-target-policy-plan.md) documents reset target and reset option policy extraction while leaving DB lookup, git reconciliation, stop, and process-drop side effects in `reset_session_to_process`.
- Plan: [rb-001-local-commit-message-policy-plan.md](rb-001-local-commit-message-policy-plan.md) documents local runtime commit-message policy extraction while leaving `CodingAgentTurn` lookup and logging in `LocalContainerService::get_commit_message`.
- Plan: [rb-001-queued-prompt-image-policy-plan.md](rb-001-queued-prompt-image-policy-plan.md) documents queued follow-up prompt image Markdown composition extraction while leaving queued-message validation and action construction in `start_queued_follow_up`.
- Plan: [rb-001-executor-exit-signal-policy-plan.md](rb-001-executor-exit-signal-policy-plan.md) documents executor exit-signal status mapping extraction while leaving process cleanup, DB updates, commit/next-action logic, queued-message handling, and finalization in `spawn_exit_monitor`.
- Plan: [rb-001-commit-eligibility-policy-plan.md](rb-001-commit-eligibility-policy-plan.md) documents post-run commit eligibility extraction while leaving commit-message lookup, repo status checks, and commit execution in `try_commit_changes`.
- Plan: [rb-001-stop-in-review-policy-plan.md](rb-001-stop-in-review-policy-plan.md) documents stop-time task InReview policy extraction while leaving DB context loading, stop side effects, and status persistence in `stop_execution`.
- Plan: [rb-001-start-status-policy-plan.md](rb-001-start-status-policy-plan.md) documents start-time status and archive policy extraction while leaving task lookup, persistence, process creation, and executor startup in `start_execution`.
- Plan: [rb-001-orphan-in-review-policy-plan.md](rb-001-orphan-in-review-policy-plan.md) documents startup orphan cleanup InReview eligibility extraction while leaving DB traversal, failure persistence, after-head capture, and status updates in `cleanup_orphan_executions`.
- Plan: [rb-001-action-prompt-policy-plan.md](rb-001-action-prompt-policy-plan.md) documents action-to-coding-agent-turn prompt extraction while leaving `CodingAgentTurn::create`, process creation, and executor startup in `start_execution`.
- Plan: [rb-001-start-error-message-policy-plan.md](rb-001-start-error-message-policy-plan.md) documents setup-required start-error message extraction while leaving failure persistence, log append, and conversation patch append in `start_execution`.
- Plan: [rb-001-log-normalization-target-policy-plan.md](rb-001-log-normalization-target-policy-plan.md) documents action-to-log-normalization target extraction while leaving msg-store lookup, QA mock invocation, executor cache lookup, and tracing in `start_execution`.
- Plan: [rb-001-log-replay-normalization-target-plan.md](rb-001-log-replay-normalization-target-plan.md) documents replay/start log-normalization target unification while preserving replay-side msg-store construction, executor fallback lookup, and stream filtering.
- Plan: [rb-001-local-approval-service-policy-plan.md](rb-001-local-approval-service-policy-plan.md) documents local runtime approval-service policy extraction while leaving bridge/noop service construction, DB handles, notification service, environment assembly, process spawn, child tracking, cancellation storage, and exit monitor setup in `start_execution_inner`.
- Plan: [rb-001-commit-delta-policy-plan.md](rb-001-commit-delta-policy-plan.md) documents post-exit commit-delta inspection policy extraction while leaving repository-state reads, git HEAD checks, commit execution, next-action startup, and finalization in `LocalContainerService`.
- Plan: [rb-001-stop-exit-code-policy-plan.md](rb-001-stop-exit-code-policy-plan.md) documents manual stop exit-code policy extraction while leaving completion persistence, child lookup, cancellation, process-group killing, message-store cleanup, DB-stream waiting, task status updates, and after-head capture in `stop_execution`.
- Plan: [rb-001-stop-cleanup-side-effects-plan.md](rb-001-stop-cleanup-side-effects-plan.md) documents the first service-level stop cleanup pass, locking the no-child stop path before grouping duplicated message-store and DB-stream cleanup side effects.
- Plan: [rb-001-stop-task-status-side-effects-plan.md](rb-001-stop-task-status-side-effects-plan.md) documents stop-time task-status persistence grouping, with service-level locks for coding-agent and dev-server no-child stops before removing duplicated DB update blocks.
- Plan: [rb-001-stop-after-head-side-effects-plan.md](rb-001-stop-after-head-side-effects-plan.md) documents stop-time after-head capture repair for missing-child stops before grouping common stop-finish side effects.
- Plan: [rb-001-finalize-status-side-effects-plan.md](rb-001-finalize-status-side-effects-plan.md) documents finalize/orphan session-task status persistence grouping while leaving notification policy and orphan eligibility unchanged.
- Plan: [rb-001-try-stop-orchestration-plan.md](rb-001-try-stop-orchestration-plan.md) documents service-level `try_stop` selection coverage before extracting per-session stop traversal.
- Plan: [rb-001-reset-stop-drop-tail-plan.md](rb-001-reset-stop-drop-tail-plan.md) documents reset stop/drop tail extraction after service-level reset behavior coverage.
- Plan: [rb-001-start-failure-side-effects-plan.md](rb-001-start-failure-side-effects-plan.md) documents service-level startup failure side-effect coverage before grouping failed-start persistence and logs.
- Plan: [rb-001-start-process-record-plan.md](rb-001-start-process-record-plan.md) documents startup execution-process record and repo-state creation extraction after extending the service-level startup failure lock.
- Plan: [rb-001-start-coding-turn-plan.md](rb-001-start-coding-turn-plan.md) documents coding-agent turn persistence extraction after the startup failure lock proved prompt persistence before executor spawn.
- Plan: [rb-001-start-success-log-stream-plan.md](rb-001-start-success-log-stream-plan.md) documents success-path log normalization and DB stream setup extraction after adding a service-level script startup lock.
- Plan: [rb-001-start-execution-env-plan.md](rb-001-start-execution-env-plan.md) documents local runtime execution-environment assembly extraction after extending the success-path script startup lock to prove selected `VK_*` variables reach the child process.
- Plan: [rb-001-start-spawn-registration-plan.md](rb-001-start-spawn-registration-plan.md) documents spawned-child lifecycle registration extraction after extending the success-path script startup lock to prove child, message-store, DB-stream, and exit-monitor handles are registered.
- Plan: [rb-001-start-approval-service-construction-plan.md](rb-001-start-approval-service-construction-plan.md) documents local runtime approval-service construction extraction while keeping bridge/noop eligibility policy in `process_completion`.
- Plan: [rb-001-start-workspace-setup-plan.md](rb-001-start-workspace-setup-plan.md) documents start-workspace setup orchestration policy extraction while keeping DB lookups, action construction, and process starts in the container service.
- Plan: [rb-001-after-head-capture-plan.md](rb-001-after-head-capture-plan.md) documents execution-process repo `after_head_commit` capture consolidation while preserving stop/orphan/finalization side effects.
- Fix: extracted pure finalization and next-action run-reason policy into `crates/services/src/services/container_workflow.rs`, then wired `ContainerService::should_finalize` and `ContainerService::try_start_next_action` through those helpers without changing DB, notification, process-spawn, or log-stream side effects.
- Behavior lock: added unit coverage for DevServer finalization suppression, setup-script/no-next suppression, failed/killed finalization precedence after earlier guards, completed/no-next finalization, script-to-script setup chaining, coding/review-to-script cleanup chaining, and coding/review next-action classification.
- Verification: `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `pnpm run check`; `pnpm run lint`.
- Fix: extracted local exit-watcher decision policy into `crates/local-deployment/src/process_completion.rs`, covering process exit decoding, commit/next-action eligibility, coding-agent change gating, and queued-message execution gating. The watcher still owns DB writes, commit execution, scratch deletion, finalization, message-store cleanup, and child-handle cleanup.
- Behavior lock: added unit coverage for successful exit mapping, watcher wait errors, successful commit/next eligibility, cleanup-script completion after failed exit, coding-agent next-action gating by committed or authored commits, non-coding next-action allowance, and queued-message suppression for failed/killed processes.
- Verification: `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `pnpm run check`; `pnpm run lint`.
- Fix: extracted stop-selection policy into `container_workflow::should_stop_execution`, leaving DB traversal and `stop_execution` side effects in `ContainerService::try_stop`.
- Behavior lock: added unit coverage that only running processes are stop candidates and dev-server processes are skipped unless `include_dev_server` is true.
- Verification: `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `pnpm run check`; `pnpm run lint`.
- Fix: extracted setup/cleanup/archive script action-chain construction into `crates/services/src/services/container_actions.rs`, made local deployment and Tauri command callers consume the same explicit helper, and removed those pure builders from the `ContainerService` trait surface.
- Behavior lock: added unit coverage for cleanup chain repo-order preservation, archive script context, manual setup chain order, no-script `None` cases, standalone setup action fields, and sequential setup chains running setup scripts in repo order before the coding action.
- Verification: `cargo test -p services container_actions --lib`; `pnpm run check`; `pnpm run lint`.
- Fix: extracted finalize notification title/message policy into `container_workflow::completion_notification`, leaving session/task status updates, warning behavior, and notification delivery in `finalize_task`.
- Behavior lock: added pure unit coverage for completed and failed notification messages plus killed/running notification suppression.
- Verification: `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: extracted reset target commit selection and reset option construction into `container_workflow::reset_target_oid` and `container_workflow::reset_options`, leaving all DB and git side effects in `reset_session_to_process`.
- Behavior lock: added pure unit coverage proving process `before_head_commit` wins over previous `after_head_commit`, previous `after_head_commit` is used as fallback, missing commits skip reset, and `log_skip_when_dirty` follows `perform_git_reset`.
- Verification: `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: extracted local runtime commit-message selection into `process_completion::commit_message_for_execution`, leaving DB lookup, error logging, repo status checks, commit execution, and next-action/finalization flow in `LocalContainerService`.
- Behavior lock: added unit coverage for coding-agent summary passthrough, current empty-summary passthrough, coding-agent fallback message, cleanup-script message, and generic execution-process message.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on missing helper; after implementation, `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved queued follow-up prompt/image Markdown composition into `process_completion::prompt_with_queued_images`, leaving executor selection, latest session lookup, cleanup-action chaining, and process start in `LocalContainerService::start_queued_follow_up`.
- Behavior lock: added unit coverage for no-image prompt preservation, blank image filtering, path trimming, two-newline message/image separation, and blank-message image-only prompts.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on missing helper; after implementation, `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved platform-specific success/failure `ExitStatus` construction and executor signal mapping into `process_completion::executor_signal_exit_status`, leaving child process-group termination and async orchestration in `spawn_exit_monitor`.
- Behavior lock: added unit coverage proving executor success maps to completed exit code 0, executor failure maps to failed non-zero exit, and a closed executor signal channel preserves the existing assumed-success behavior.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on missing helper; after implementation, `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved post-run commit eligibility into `process_completion::should_try_commit_changes`, leaving commit-message lookup, workspace path derivation, repo status checks, and actual commits in `LocalContainerService::try_commit_changes`.
- Behavior lock: added unit coverage proving only `CodingAgent` and `CleanupScript` run reasons attempt post-run commits; setup scripts and dev servers still skip commit checks.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on missing helper; after implementation, `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved stop-time task InReview policy into `process_completion::should_mark_task_in_review_after_stop`, leaving DB context loading, process stop, message-store cleanup, and `Task::update_status` persistence in `LocalContainerService::stop_execution`.
- Behavior lock: added unit coverage proving stopped dev servers do not mark tasks InReview, while stopped coding-agent, setup-script, and cleanup-script processes still do.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on missing helper; after implementation, `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved start-time session/task InProgress and workspace unarchive decisions into `container_workflow::should_mark_session_in_progress_on_start`, `container_workflow::should_mark_task_in_progress_on_start`, and `container_workflow::should_unarchive_workspace_on_start`, leaving all DB reads/writes, process record creation, coding-agent-turn creation, and executor startup in `ContainerService::start_execution`.
- Behavior lock: added unit coverage proving non-dev starts mark sessions InProgress, dev-server starts skip session/task progress updates, already-InProgress tasks are not redundantly updated, archive-script starts preserve archive state, and other run reasons unarchive workspaces.
- Verification: red `cargo test -p services container_workflow --lib` first failed on missing helpers; after implementation, `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved startup orphan cleanup InReview eligibility into `container_workflow::should_mark_session_in_review_after_orphan_cleanup`, leaving orphan failure persistence, context traversal, after-head commit capture, and session/task status writes in `cleanup_orphan_executions`.
- Behavior lock: added unit coverage proving orphaned coding-agent, setup-script, and cleanup-script processes mark session/task InReview, while dev-server and archive-script orphan cleanup skips those status updates.
- Verification: red `cargo test -p services container_workflow --lib` first failed on the missing helper; after implementation, `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved action-to-coding-agent-turn prompt selection into `container_workflow::coding_agent_turn_prompt`, leaving `CodingAgentTurn::create`, process record creation, repo-state capture, and executor startup in `ContainerService::start_execution`.
- Behavior lock: added unit coverage proving coding-agent initial, coding-agent follow-up, and review actions expose prompts for turn creation, while script actions do not create coding-agent turns.
- Verification: red `cargo test -p services container_workflow --lib` first failed on the missing helper; after implementation, `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved missing-executable setup-required normalized error entry construction into `container_workflow::missing_executable_start_error_entry`, leaving `ExecutionProcessLogs::append_log_line`, `ConversationPatch::add_normalized_entry`, failed-process persistence, and session/task InReview updates in `ContainerService::start_execution`.
- Behavior lock: added unit coverage proving the entry uses `NormalizedEntryError::SetupRequired`, preserves the executable-specific message text, and keeps timestamp/metadata empty.
- Verification: red `cargo test -p services container_workflow --lib` first failed on the missing helper; after implementation, `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved action-to-log-normalization target selection into `container_workflow::log_normalization_target`, leaving msg-store lookup, QA-mode normalization, executor cache lookup, and missing-profile tracing in `ContainerService::start_execution`.
- Behavior lock: added unit coverage proving coding-agent initial, follow-up, and review actions produce normalization targets with the action executor profile and effective working directory, while script actions do not start log normalization.
- Verification: red `cargo test -p services container_workflow --lib` first failed on the missing helper; after implementation, `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: unified historical/raw log replay normalization with `container_workflow::log_normalization_target`, removing a duplicated action-type match in `stream_raw_logs_to_db` and correcting review replay normalization to respect `ReviewRequest::effective_dir` just like live startup normalization.
- Behavior lock: existing helper coverage proves review actions with `working_dir` resolve to the effective subdirectory, and script actions still do not normalize. The replay path now consumes that same helper while preserving replay-specific executor fallback behavior.
- Verification: `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; first `pnpm run lint` caught a needless borrow in the replay helper call; after fixing it, `pnpm run check`; `pnpm run lint`.
- Fix: moved local runtime approval-service selection policy into `process_completion::should_create_executor_approval_bridge`, leaving `ExecutorApprovalBridge` and `NoopExecutorApprovalService` construction in `LocalContainerService::start_execution_inner`.
- Behavior lock: added unit coverage proving Codex, Claude Code, and Opencode actions use the approval bridge path, while absent base executors such as script actions stay on the noop path.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on the missing helper; after implementation, process-completion tests passed with 21 tests; first `cargo fmt --check` caught indentation drift; after `cargo fmt`, `cargo test -p local-deployment process_completion --lib`, `cargo check -p local-deployment`, `cargo fmt --check`, `pnpm run check`, and `pnpm run lint` passed.
- Fix: moved post-exit commit-delta inspection eligibility into `process_completion::should_inspect_commits_from_execution`, leaving `has_commits_from_execution`, post-run commit handling, next-action startup, and manual finalization inside the local runtime exit monitor.
- Behavior lock: added unit coverage proving only coding-agent runs inspect execution commit deltas; setup scripts, cleanup scripts, dev servers, and archive scripts skip that repository HEAD comparison path.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on the missing helper; after implementation, process-completion tests passed with 22 tests; first `cargo fmt --check` caught wrapping drift; after `cargo fmt`, `cargo test -p local-deployment process_completion --lib`, `cargo check -p local-deployment`, `cargo fmt --check`, `pnpm run check`, and `pnpm run lint` passed.
- Fix: moved manual stop-path exit-code selection into `process_completion::stop_exit_code_for_status`, leaving `ExecutionProcess::update_completion` and all process cleanup side effects in `LocalContainerService::stop_execution`.
- Behavior lock: added unit coverage proving manual `Completed` stops persist exit code `0`, while `Failed`, `Killed`, and `Running` stop statuses persist no exit code.
- Verification: red `cargo test -p local-deployment process_completion --lib` first failed on the missing helper; after implementation, process-completion tests passed with 24 tests; first `cargo fmt --check` caught assertion wrapping drift; after `cargo fmt`, `cargo test -p local-deployment process_completion --lib`, `cargo check -p local-deployment`, `cargo fmt --check`, `pnpm run check`, and `pnpm run lint` passed.
- Fix: grouped repeated stop-path message-store and DB-stream cleanup into `LocalContainerService::finish_msg_store_and_db_stream`, leaving stop completion persistence, child lookup, cancellation, process-group killing, task status updates, and after-head capture in `stop_execution`.
- Behavior lock: added a service-level local-runtime test proving the no-child stop path updates the execution process to killed, removes the in-memory message store and DB-stream handle, appends `Finished` to the retained store, waits for the DB-stream handle, and marks the linked task `InReview`.
- Verification: `cargo test -p local-deployment stop_execution_without_child_cleans_logs_and_marks_task_in_review --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: grouped repeated stop-time task-status persistence into `LocalContainerService::mark_task_in_review_after_stop_if_needed`, leaving `stop_execution` responsible for process completion, child cleanup, cancellation, stream cleanup, and after-head capture.
- Behavior lock: kept the coding-agent no-child stop service test and added a dev-server no-child stop service test proving dev-server cleanup still kills/completes the process and finishes the message store without moving the linked task out of `InProgress`.
- Verification: `cargo test -p local-deployment stop_execution_without_child_leaves_dev_server_task_status_unchanged --lib`; `cargo test -p local-deployment stop_execution_without_child_cleans_logs_and_marks_task_in_review --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: grouped common stop-finish side effects into `LocalContainerService::finish_stopped_execution` and made the missing-child stop path record best-effort `after_head_commit` just like the child-handle path.
- Behavior lock: added a service-level missing-child stop test with a real git repository and execution repo-state row; it failed before the fix with `after_head_commit = None` and now proves the current HEAD is persisted.
- Verification: red `cargo test -p local-deployment stop_execution_without_child_records_after_head_commit --lib` first failed on missing after-head capture; after implementation, `cargo test -p local-deployment stop_execution_without_child_records_after_head_commit --lib`; `cargo test -p local-deployment stop_execution_without_child_cleans_logs_and_marks_task_in_review --lib`; `cargo test -p local-deployment stop_execution_without_child_leaves_dev_server_task_status_unchanged --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p local-deployment`; initial `cargo fmt --check` caught import order drift, then `cargo fmt` was applied; final `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: grouped duplicated session/task `InReview` persistence into `ContainerService::mark_session_and_task_in_review`, reused by `finalize_task` and startup orphan cleanup while preserving completion notification construction/delivery and orphan run-reason eligibility.
- Behavior lock: added service-level local-runtime coverage proving completed coding-agent finalization marks both session and task `InReview`, and startup orphan cleanup marks both session and task `InReview` for orphaned coding-agent executions.
- Verification: `cargo test -p local-deployment finalize_task_marks_session_and_task_in_review --lib`; `cargo test -p local-deployment new_marks_orphaned_coding_agent_session_and_task_in_review --lib`; `cargo test -p local-deployment new_marks_orphaned_running_executions_failed --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; initial `cargo fmt --check` caught added-test wrapping drift, then `cargo fmt` was applied; final `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `ContainerService::try_stop_session_processes` to separate per-session process loading, stop selection, and stop invocation from workspace session traversal while preserving the existing error-tolerant behavior and `stop_execution` side effects.
- Behavior lock: added a service-level local-runtime test proving `try_stop(workspace, false)` kills running non-dev executions, skips running dev-server executions, and leaves completed executions unchanged across real DB session/process rows.
- Verification: `cargo test -p local-deployment try_stop_without_dev_server_stops_only_running_non_dev_processes --lib`; `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; initial `cargo fmt --check` caught added-test wrapping drift, then `cargo fmt` was applied; `pnpm run check`; `pnpm run lint`.
- Fix: extracted `ContainerService::finish_reset_to_process` for the reset tail that stops workspace executions and soft-drops target-session rows, leaving reset context loading, git reset target selection, dirty-state handling, and worktree reconciliation in `reset_session_to_process`.
- Behavior lock: added a service-level local-runtime reset test proving reset stops running non-dev executions, keeps dev-server executions running, soft-drops the target and later target-session processes, and leaves pre-target processes visible.
- Verification: `cargo test -p local-deployment reset_session_to_process_stops_workspace_and_drops_target_tail --lib`; `cargo test -p local-deployment try_stop_without_dev_server_stops_only_running_non_dev_processes --lib`; `cargo test -p services container_workflow --lib`; `cargo check -p services -p local-deployment`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: extracted `ContainerService::finish_failed_start` for failed-start process completion, session/task `InReview` rollback, stderr log append, and missing-executable normalized setup-required patch append, leaving startup context loading, process creation, turn creation, success startup, normalization, and DB stream setup in `start_execution`.
- Behavior lock: added a service-level local-runtime startup failure test using an unknown executor variant so no external agent is spawned; it proves `start_execution` creates the execution process and coding-agent turn, unarchives the workspace, then marks the process failed, returns session/task to `InReview`, and appends the startup failure stderr log.
- Verification: `cargo test -p local-deployment start_execution_unknown_executor_marks_failed_and_restores_review_state --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `ContainerService::create_start_execution_process` for startup execution-process creation and repository before-head capture, leaving start status persistence, workspace unarchive, coding-turn creation, executor spawn, failure cleanup, normalization, and DB stream setup in `start_execution`.
- Behavior lock: extended the startup failure service test to prove the process keeps the requested coding-agent run reason and persists exactly one repo-state row with the workspace repo id and empty before/after/merge commits for the non-git test repo.
- Verification: `cargo test -p local-deployment start_execution_unknown_executor_marks_failed_and_restores_review_state --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `ContainerService::create_start_coding_agent_turn` for optional startup coding-agent turn persistence, leaving prompt eligibility in `container_workflow`, and leaving process creation, workspace unarchive, executor spawn, failure cleanup, normalization, and DB stream setup in `start_execution`.
- Behavior lock: reused the startup failure service test that proves a coding-agent request persists a `CodingAgentTurn` with the prompt before executor spawn failure. Existing `container_workflow` tests continue to lock which action types expose prompts.
- Verification: `cargo test -p local-deployment start_execution_unknown_executor_marks_failed_and_restores_review_state --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `ContainerService::start_success_log_streaming` for success-path log normalization and DB stream handle setup, leaving executor spawn, child tracking, exit monitor setup, failed-start cleanup, and stop cleanup unchanged.
- Behavior lock: added a service-level local-runtime script startup test that uses a long-running script action to avoid external agent binaries, proving successful `start_execution` creates the execution process, in-memory message store, and DB stream handle before stopping the process for cleanup. Existing `container_workflow` tests continue to lock normalization target selection.
- Verification: `cargo test -p local-deployment start_execution_script_stores_db_stream_handle_on_success --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `LocalContainerService::build_execution_env` for local runtime repo context, commit-reminder config, task/project lookup, and `VK_*` variable assembly, leaving approval-service selection, executor spawn timeout, child tracking, cancellation storage, and exit monitor setup in `start_execution_inner`.
- Behavior lock: extended the service-level local-runtime script startup test so the actual child process prints selected `VK_*` variables, proving project name, workspace id, and session id are injected through the extracted environment builder before cleanup stops the process.
- Verification: `cargo test -p local-deployment start_execution_script_stores_db_stream_handle_on_success --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `LocalContainerService::register_spawned_execution` for successful spawn lifecycle registration, grouping message-store stream setup, child storage, optional cancellation token storage, and exit-monitor handle storage behind one runtime lifecycle helper while leaving the spawn timeout and `ExecutorAction::spawn` call in `start_execution_inner`.
- Behavior lock: extended the service-level local-runtime script startup test to prove a successful start registers the child handle, message store, DB stream handle, and exit monitor handle before the process is stopped for cleanup.
- Verification: `cargo test -p local-deployment start_execution_script_stores_db_stream_handle_on_success --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `LocalContainerService::create_executor_approval_service` for approval bridge/noop object construction, leaving `process_completion::should_create_executor_approval_bridge` as the single policy owner and leaving spawn behavior unchanged.
- Behavior lock: reused the approval-bridge policy tests proving interactive coding executors choose the bridge path and non-interactive actions choose noop, plus the service-level script startup lock proving non-interactive startup still spawns and registers successfully.
- Verification: `cargo test -p local-deployment approval_bridge --lib`; `cargo test -p local-deployment start_execution_script_stores_db_stream_handle_on_success --lib`; `cargo test -p services container_workflow --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo test -p local-deployment process_completion --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted start-workspace setup start-mode selection into `container_actions::workspace_setup_start_mode`, making direct-coding, all-parallel setup, and sequential setup-chain cases explicit instead of relying on `Iterator::all` empty-list behavior inside `start_workspace_with_session`.
- Behavior lock: added direct `container_actions` unit coverage for no setup scripts selecting direct coding, all setup scripts marked parallel selecting parallel setup before coding, and any non-parallel setup selecting the sequential setup chain.
- Verification: `cargo test -p services container_actions --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo check -p services -p local-deployment`; initial `cargo fmt --check` caught helper wrapping/import order drift, then `cargo fmt` was applied; final `cargo fmt --check`; `pnpm run check`; `pnpm run backend:lint`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted `ContainerService::record_after_head_commits` so orphan cleanup and local runtime completion now share the same repo-path/head-capture/update helper instead of each encoding execution-process repo-state updates separately.
- Behavior lock: added a service-level local-runtime orphan cleanup test proving a running orphaned process is marked failed and records the current repo HEAD in `execution_process_repo_states.after_head_commit`; existing stop-without-child coverage continues to prove stopped executions record the same field.
- Verification: `cargo test -p local-deployment cleanup_orphan_executions_records_after_head_commit --lib`; `cargo test -p local-deployment stop_execution_without_child_records_after_head_commit --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check`.
- Remaining cleanup: the trait still owns DB-heavy async orchestration, but `start_execution` is now mostly a readable sequence of status, process record, workspace, turn, spawn, failure, and success-log steps, while `start_execution_inner` now reads as container ref, repo/current-dir, approval service, environment, spawn, register with construction details hidden behind named helpers. After-head capture drift is closed; further RB-001 work should continue from fresh smell review rather than carving the same start path by habit.

### RB-002: Workspace/worktree path rules are duplicated across service and local runtime layers

- Category: duplication, historical baggage, weak boundary
- Severity: high
- Confidence: high
- Files: `crates/services/src/services/container.rs`, `crates/local-deployment/src/container.rs`
- Evidence: generic service helpers and local runtime helpers both encode `use_worktree`, repo-root detection, workspace base, and `agent_working_dir` rules.
- Risk: path derivation can diverge between scheduling/orchestration and local deployment.
- Suggested behavior lock: table-driven tests for `use_worktree=false`, app-owned workspace dirs, direct external worktrees, and `agent_working_dir` that starts with the repo name.
- Cleanup status: pass 4 completed.
- Plan: [rb-002-direct-worktree-base-dir-contract-plan.md](rb-002-direct-worktree-base-dir-contract-plan.md) documents the local-runtime test reinforcement slice and explicitly defers helper collapse until `ensure_container_exists` / discovery integration behavior is locked.
- Plan: [rb-002-ensure-container-contract-plan.md](rb-002-ensure-container-contract-plan.md) documents the integration contract slice for configured external worktrees, missing external paths, and existing-worktree discovery.
- Plan: [rb-002-local-base-helper-consolidation-plan.md](rb-002-local-base-helper-consolidation-plan.md) documents the shared pure-helper consolidation after the local-runtime behavior locks were in place.
- Fix: extracted `crates/services/src/services/workspace_paths.rs` for pure workspace path policy covering service workspace base-dir derivation, agent working-dir normalization, and local runtime repo path derivation.
- Behavior lock: added table-driven unit coverage for non-worktree repo path use, container refs pointing at repo roots, agent working-dir repo prefix strip/add behavior, direct checkout roots, agent dirs targeting repo folders, and agent dirs targeting subdirectories.
- Verification: `cargo test -p services workspace_paths --lib`; `cargo check -p services -p local-deployment`; `pnpm run check`; `pnpm run backend:lint`.
- Behavior lock: added local runtime unit coverage for non-worktree base-dir repo path use, worktree `container_ref` preservation, direct external single-repo checkout detection, VibeX-owned workspace exclusion, repo-prefixed agent working-dir exclusion, and git-checkout-root repo-path preservation.
- Verification: `cargo test -p local-deployment direct_external_worktree --lib`; `cargo test -p local-deployment workspace_ --lib`; `cargo test -p local-deployment container::tests --lib`.
- Behavior lock: added async local runtime coverage proving `ensure_container_exists` reuses configured direct external worktree roots without creating repo subdirectories, rejects missing direct external paths, discovers existing git worktrees by branch, and writes discovered container refs back to the DB. The discovery test compares canonical paths to avoid Windows short-name/long-name false failures.
- Verification: `cargo test -p local-deployment ensure_container_exists --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Fix: moved local-runtime workspace base-dir policy into `workspace_paths::local_runtime_workspace_base_dir` and wired `LocalContainerService::normalized_workspace_base_dir` through it, while preserving service execution base-dir behavior separately.
- Behavior lock: added shared-helper coverage proving local runtime uses repo paths without worktrees, keeps direct checkout roots as the runtime base, and keeps multi-repo workspace containers unchanged. Existing local container tests continue to cover direct external runtime and discovery behavior.
- Verification: `cargo test -p services workspace_paths --lib`; `cargo test -p local-deployment container::tests --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Remaining cleanup: RB-002 duplication is now materially reduced. Further cleanup should only target naming/documentation or broader workspace-manager integration if a concrete duplicated policy remains after fresh review.

### RB-003: Git service remains a split-brain facade over CLI and libgit2

- Category: complex implementation, duplication, weak boundary
- Severity: high
- Confidence: high
- Files: `crates/git/src/lib.rs`, `crates/git/src/cli.rs`
- Evidence: `cli.rs` documents CLI-for-mutation policy, while `GitService` remains a large facade that wraps CLI mutations and keeps libgit2/network logic.
- Risk: command behavior, auth/network behavior, and service policy are hard to reason about as separate concerns.
- Suggested behavior lock: focused tests around mutation command construction, read/network behavior, and worktree operations before splitting facade responsibilities.
- Cleanup status: pass 10 completed.
- Fix: extracted public DTOs, option/result structs, `GitServiceError`, and `DiffTarget`/`Commit` wrappers into `crates/git/src/types.rs`, re-exported from `crates/git/src/lib.rs`. This removes UI/API response type declarations and corrupted historical banner comments from the service implementation file without changing public imports.
- Fix: extracted Git panel/staging/log/commit operations into `crates/git/src/panel_ops.rs`, including the related status formatting, binary/image detection, numstat parsing, and untracked-file line counting helpers. `GitService::read_file_to_string` is now `pub(crate)` so the helper remains internal to the crate while allowing the panel module to reuse the existing repository-relative file read policy.
- Fix: extracted worktree operations into `crates/git/src/worktree_ops.rs`, including branch checkout path lookup, worktree status/count wrappers, reset/reconcile policy, add/remove/move/prune wrappers, and branch listing worktree annotations. The public `GitService` API remains unchanged, while `lib.rs` no longer owns worktree bookkeeping and branch-list presentation policy.
- Fix: extracted branch workflow orchestration into `crates/git/src/branch_workflow_ops.rs`, including squash merge, in-memory squash commit creation, rebase, and rebase-back conflict/divergence handling. This separates mutation workflow sequencing from read-only branch graph and repository query methods without changing `GitService` callers.
- Fix: extracted branch metadata operations into `crates/git/src/branch_metadata_ops.rs` and conflict-state commands into `crates/git/src/conflict_ops.rs`, including branch delete/type/existence/rename helpers plus rebase/conflict detection, conflicted-file listing, continue, and abort behavior.
- Fix: extracted remote/network operations into `crates/git/src/remote_ops.rs`, including default remote resolution, remote branch status refresh, remote URL/listing helpers, branch fetch, start-point refresh, remote resolution, push tracking updates, fetch refspec helpers, and cloud clone support.
- Fix: extracted recent-file ranking statistics into `crates/git/src/stats_ops.rs`, removing commit-walk/file-stat aggregation from the main service implementation.
- Fix: extracted read-only diff query logic into `crates/git/src/diff_ops.rs`, including worktree/branch/commit diff entry points, libgit2 diff conversion, sparse-checkout-safe CLI status diff conversion, inline-content size/binary guards, permission-only change detection, and repository-relative file reading shared with panel diff helpers.
- Fix: extracted branch/head/graph query logic into `crates/git/src/branch_query_ops.rs`, including branch ahead/behind status, branch commit messages, commit graph construction, merge-base lookup, HEAD/current branch/OID queries, fork-point lookup, commit subject lookup, and branch resolution. Worktree cleanliness guards were moved into `crates/git/src/worktree_ops.rs`, where the worktree policy belongs.
- Plan: [rb-003-init-identity-cleanup-plan.md](rb-003-init-identity-cleanup-plan.md) documents the residual repository initialization and commit identity extraction after the larger Git facade split.
- Fix: extracted repository initialization, initial commit creation, CLI commit identity setup, libgit2 signature fallback, repository opening, and the public `commit` entry point into `crates/git/src/init_ops.rs`. The public `GitService` API is unchanged, while `lib.rs` no longer owns initialization or identity policy.
- Correctness fix: `ensure_cli_commit_identity` now opens the local config level and fills missing `user.name` / `user.email` independently, so adding a missing email no longer overwrites an existing local name and global config cannot suppress the repo-local fallback identity.
- Behavior lock: added unit coverage proving initialization creates `main` with an initial commit, `ensure_main_branch_exists` creates the empty-repo commit once, and CLI identity setup preserves an existing local `user.name` while filling a missing local email.
- Behavior lock: used the existing Git crate regression suite before and after the move; it covers branch/worktree status, dirty-worktree safety, merge/rebase safety, push/fetch behavior, sparse-checkout diffs, and workflow operations.
- Verification: `cargo test -p git init_identity --lib`; `cargo test -p git`; `cargo check -p git`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`.
- Remaining cleanup: the original split-brain `GitService` god-file problem is substantially reduced. The remaining `lib.rs` now contains service construction, branch-name validation, and shared CLI conflict-summary helpers used by branch workflow operations. Further cleanup should target those helpers only if a clearer owner emerges.

### RB-004: ACP log normalization does parsing, formatting, task inference, and stream state in one file

- Category: complex implementation, weak boundary, missing tests
- Severity: high
- Confidence: medium-high
- Files: `crates/executors/src/executors/acp/normalize_logs.rs`, `crates/executors/src/executors/acp/client.rs`, `crates/executors/src/executors/acp/provider.rs`, `crates/executors/src/executors/acp/harness.rs`
- Evidence: normalization entry points sit beside markdown formatting, tool-result extraction, task-create inference, partial tool-call state, and parser machinery.
- Risk: pure formatting changes can accidentally alter parser semantics, and helper-oriented tests do not prove real ACP event sequences end to end.
- Suggested behavior lock: one end-to-end ACP fixture for `normalize_logs*`, plus explicit assertions for task-create extraction, cumulative streaming text replacement, and image/resource markdown rendering.
- Cleanup candidate: split pure formatting/extraction helpers first, before touching parser behavior.
- Cleanup status: fixed in this pass for pure formatting, task inference, parser, streaming-text, partial tool-call state boundaries, approval-feedback construction, ACP client permission-outcome mapping, ACP provider permission-policy mapping, and ACP harness approval-cancel policy.
- Plan: [rb-004-approval-feedback-plan.md](rb-004-approval-feedback-plan.md) documents request-permission/denied-approval feedback coverage before moving denial feedback entry construction out of the ACP event loop.
- Plan: [rb-004-permission-outcome-plan.md](rb-004-permission-outcome-plan.md) documents ACP client permission option selection and approval-status outcome mapping extraction before changing `AcpClient::request_permission`.
- Plan: [rb-004-provider-permission-policy-plan.md](rb-004-provider-permission-policy-plan.md) documents ACP provider permission-policy coverage before moving approval/mode mapping out of `AcpBackedExecutor::apply_overrides`.
- Plan: [rb-004-harness-approval-cancel-plan.md](rb-004-harness-approval-cancel-plan.md) documents ACP harness approval-denial cancellation coverage before moving the predicate out of the event-forwarder loop.
- Fix: extracted `crates/executors/src/executors/acp/formatting.rs` for ACP plan/content-block Markdown rendering, `crates/executors/src/executors/acp/task_inference.rs` for task-create/subagent inference, `crates/executors/src/executors/acp/parser.rs` for ACP line parsing plus execute-command title normalization, `crates/executors/src/executors/acp/streaming.rs` for streaming text state/merge policy, and `crates/executors/src/executors/acp/tool_state.rs` for partial tool-call state accumulation. `normalize_logs.rs` now adapts event-loop data through narrow helper inputs instead of owning every policy.
- Behavior lock: added an end-to-end stdout ACP event sequence test that drives `MsgStore` through `normalize_logs_with_context_window_override` and asserts cumulative assistant replacement, task-create inference, completed tool status, raw output preservation, and token context fallback. Added parser tests for serialized ACP event lines, invalid lines, raw command precedence, and known execute-title suffix stripping. Added a tool-update sequence test proving title-less updates reuse existing tool state, replace the same normalized entry, preserve execute command semantics, and surface completed output.
- Correctness fix: replaced the old execute-title suffix parsing based on `split(...).next()` because it made the parenthetical suffix branch unreachable; the parser now uses `split_once` for the known suffixes.
- Verification: `cargo test -p executors acp::parser --lib`; `cargo test -p executors acp::normalize_logs --lib`; `cargo check -p executors`; `pnpm run check`; `pnpm run backend:lint`; `pnpm run lint`.
- Fix: extracted denied approval feedback entry construction from the ACP stdout event loop, leaving request-permission tool entry handling, approval response dispatch order, and non-denied approval statuses unchanged. Shared tool-name id parsing is now module-level so tool mapping and denial feedback use the same logic.
- Behavior lock: added an end-to-end request-permission fixture that sends a tool-call update followed by a denied approval response, proving normalization emits the pending tool entry and then a `UserFeedback` entry with the denied tool name and trimmed denial reason.
- Verification: `cargo test -p executors acp::normalize_logs --lib`; `cargo check -p executors`; `cargo fmt --check`; `pnpm run check`; `pnpm run backend:lint`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted ACP permission outcome policy from `AcpClient::request_permission`, separating auto-mode option preference and resolved approval-status mapping from the async approval service request, feedback queueing, and `ApprovalResponse` event emission.
- Behavior lock: added direct ACP client unit coverage proving no-approval auto mode prefers `allow_always` over `allow_once`, falls back to the first option when needed, approved statuses select `allow_once`, denied statuses select `reject_once`, approved-without-allow returns no outcome for invalid-request handling, and timed-out/pending statuses cancel.
- Verification: `cargo test -p executors acp::client --lib`; `cargo check -p executors`; `cargo fmt --check`; `pnpm run check`; `pnpm run backend:lint`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted ACP provider permission-policy mapping into a pure helper, leaving `AcpBackedExecutor::apply_overrides` responsible for model, agent, Codex reasoning, and applying the computed approval/mode overrides.
- Behavior lock: added direct ACP provider unit coverage proving Auto disables approval forwarding without clearing mode, Supervised enables approval forwarding without changing mode, and Plan enables approval forwarding while forcing ACP plan mode.
- Verification: `cargo test -p executors acp::provider --lib`; `cargo check -p executors`; `cargo fmt --check`; `pnpm run check`; `pnpm run backend:lint`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.
- Fix: extracted ACP harness approval-denial cancellation policy into a pure helper, leaving stdout forwarding, session-file persistence, and `CancelNotification` dispatch in the event-forwarder loop.
- Behavior lock: red `cargo test -p executors acp::harness --lib` first failed on the missing helper; after implementation, direct harness tests prove only denied approval responses with non-empty feedback trigger session cancellation, while blank/missing denial feedback and approved responses do not.
- Verification: `cargo test -p executors acp::harness --lib`; `cargo check -p executors`; `cargo fmt --check`; `pnpm run check`; `pnpm run backend:lint`; `pnpm run lint`; `git diff --check` passed with LF/CRLF warnings only.

### RB-005: Notification and DB update surfaces contain stale no-op or dead seams

- Category: dead code, historical baggage
- Severity: medium
- Confidence: high
- Files: `crates/db/src/models/execution_process.rs`, `crates/services/src/services/notification.rs`
- Evidence: `UpdateExecutionProcess` is marked `#[allow(dead_code)]` and appears unreferenced. `NotificationService` carries a no-op `send_push_notification` plus OS-specific notification helpers that appear unused in the current flow.
- Risk: stale seams obscure the real notification contract and invite future work to integrate with dead APIs.
- Cleanup status: fixed in this pass.
- Fix: removed the unreferenced `UpdateExecutionProcess` DTO, removed no-op push notification dispatch, and deleted private unused OS-native push helper functions plus their `#[allow(dead_code)]` suppressions.
- Behavior lock: reference search proved the removed symbols had no callers outside the no-op internal dispatch; `generate-types:check` proved shared generated TypeScript did not drift after deleting the `TS`-derived DTO.
- Verification: `cargo check -p db -p services`; `pnpm run generate-types:check`; `pnpm run check`; `pnpm run lint`.

### RB-006: Event service stores hook counter state it never reads

- Category: dead code, weak boundary
- Severity: medium
- Confidence: high
- Files: `crates/services/src/services/events.rs`, `crates/local-deployment/src/lib.rs`
- Evidence: `EventService` stored `entry_count` only behind `#[allow(dead_code)]`; reference search showed the active counter is owned by the `EventService::create_hook` closure, while the service instance never reads the field.
- Risk: the constructor suggested event sequencing was service-instance state when the actual lifecycle belongs to the SQLite hook closure.
- Cleanup status: fixed in this pass.
- Plan: [rb-006-event-service-dead-counter-plan.md](rb-006-event-service-dead-counter-plan.md) documents the dead-field deletion while preserving hook-owned fallback event counting.
- Fix: removed the dead `EventService` field and constructor argument, updated local deployment construction, and left `EventService::create_hook` counter ownership unchanged.
- Behavior lock: pre-edit `cargo test -p services events --lib` compiled the event-service test target with 0 matched tests; post-edit `rg` proved `#[allow(dead_code)]` was gone and `entry_count` remains only in hook-owned code.
- Verification: `cargo test -p services events --lib`; `cargo check -p services -p local-deployment`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### RB-007: Executor profile legacy deserializer has a stale deletion marker

- Category: historical baggage, weak boundary, missing tests
- Severity: medium
- Confidence: high
- Files: `crates/executors/src/profile.rs`
- Evidence: `de_base_coding_agent_kebab` was labeled "can be deleted 14 days from 3/9/25", but it is still attached to persisted serde boundaries for `ExecutorProfileId` and `ExecutorConfig`. `BaseCodingAgent` itself parses canonical `SCREAMING_SNAKE_CASE`, so deleting the helper would reject legacy `claude-code` style payloads.
- Risk: a future cleanup could follow the stale time-boxed comment and break old config, scratch, or DB action JSON reads.
- Cleanup status: fixed in this pass.
- Plan: [rb-007-executor-profile-legacy-deserializer-plan.md](rb-007-executor-profile-legacy-deserializer-plan.md) documents the compatibility-lock slice.
- Fix: replaced the stale deletion marker with an explicit persisted-payload compatibility contract.
- Behavior lock: added serde tests proving `ExecutorProfileId` and `ExecutorConfig` still accept legacy kebab-case executor ids, and that the legacy `profile` alias still maps to `executor`.
- Verification: `cargo test -p executors profile --lib`; `cargo check -p executors`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### RB-008: Filesystem watcher guesses deleted path type from extension shape

- Category: weak boundary, missing tests
- Severity: medium-high
- Confidence: high
- Files: `crates/services/src/services/filesystem_watcher.rs`
- Evidence: `path_allowed` handled missing event paths by treating extensionless names as directories, even though `notify::EventKind` already distinguishes precise file and folder create/remove events.
- Risk: deleting an extensionless file whose name matches a directory-only `.gitignore` pattern can be silently filtered out as if it were a deleted directory.
- Cleanup status: fixed in this pass.
- Plan: [rb-008-filesystem-watcher-path-kind-plan.md](rb-008-filesystem-watcher-path-kind-plan.md) documents the path-kind hint cleanup and regression scope.
- Fix: derived a narrow file/directory hint from precise `Create(File|Folder)` and `Remove(File|Folder)` events, passed it into gitignore filtering, and kept real filesystem metadata authoritative when the path still exists.
- Behavior lock: red `cargo test -p services filesystem_watcher --lib` first failed for a deleted extensionless file under a directory-only ignore rule; after implementation, the deleted file forwards while the deleted directory remains ignored.
- Verification: `cargo test -p services filesystem_watcher --lib`; `cargo check -p services`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### RB-009: Executor action working directory policy is duplicated per action type

- Category: duplication, weak execution boundary, missing tests
- Severity: medium
- Confidence: high
- Files: `crates/executors/src/actions/mod.rs`, `crates/executors/src/actions/coding_agent_initial.rs`, `crates/executors/src/actions/coding_agent_follow_up.rs`, `crates/executors/src/actions/review.rs`, `crates/executors/src/actions/script.rs`
- Evidence: initial coding-agent, follow-up, review, and script actions each implemented the same `working_dir` override policy locally.
- Risk: future changes to container-relative execution directories could drift between agent and script actions, changing where user commands run.
- Cleanup status: fixed in this pass.
- Plan: [rb-009-executor-action-working-dir-plan.md](rb-009-executor-action-working-dir-plan.md) documents the shared execution-directory policy cleanup.
- Fix: added a shared `effective_working_dir` helper in the action module and routed all four action spawn paths through it without changing profile selection, approval wiring, QA mock behavior, or script command construction.
- Behavior lock: red `cargo test -p executors actions --lib` first failed on the missing helper; after implementation, tests prove no override uses the current directory, relative overrides resolve below it, and nested components are preserved.
- Verification: `cargo test -p executors actions --lib`; `cargo check -p executors`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

### RB-010: Executor action agent resolution is duplicated per coding action

- Category: duplication, weak execution boundary, missing tests
- Severity: medium
- Confidence: high
- Files: `crates/executors/src/actions/mod.rs`, `crates/executors/src/actions/coding_agent_initial.rs`, `crates/executors/src/actions/coding_agent_follow_up.rs`, `crates/executors/src/actions/review.rs`
- Evidence: initial coding-agent, follow-up, and review actions each performed the same profile lookup, executor override application, and approval-service attachment before dispatching action-specific spawn calls.
- Risk: executor profile, override, or approval wiring can drift between action types, producing different runtime behavior for logically identical executor selection.
- Cleanup status: fixed in this pass.
- Plan: [rb-010-executor-action-agent-resolution-plan.md](rb-010-executor-action-agent-resolution-plan.md) documents the shared agent-resolution boundary cleanup.
- Fix: added a shared `configured_coding_agent` helper in the action module and routed initial, follow-up, and review actions through it while preserving QA mock behavior, follow-up reset handling, and review session handling.
- Behavior lock: red `cargo test -p executors actions --lib` first failed on the missing helper; after implementation, tests prove configured Codex overrides are applied and missing profile variants return an `UnknownExecutorType` containing the requested profile id.
- Verification: `cargo test -p executors actions --lib`; `cargo check -p executors`; `cargo fmt --check`; `pnpm run check`; `pnpm run lint`; `git diff --check`.

## Safest Cleanup Candidates

1. RB-001 container runtime abstraction. The remaining higher-value backend cleanup is behavior-locking DB-heavy async transition methods, not deleting the trait or moving side effects without tests.
2. Fresh backend/runtime review. RB-002, RB-003, RB-004, RB-005, RB-006, RB-007, RB-008, RB-009, and RB-010 have all had their currently identified safe cleanup slices completed; do not keep mining those entries without a new concrete duplicated policy, stale branch, or behavior gap.

## Uncertainties

- RB-001 should not be collapsed until external/deployment extension plans are checked. If future non-local implementations are planned, the cleanup should continue moving workflow logic out of the trait instead of deleting the abstraction.
- RB-002 direct external worktree helper and `ensure_container_exists` semantics are locked, and the local base-dir policy now lives in the shared path module. No further RB-002 refactor should be attempted without a fresh duplicated-policy finding.
- RB-004 still has a large event loop, but its helper policies are now separated. Further event-loop splitting should wait for broader approval-response and request-permission fixtures, not because the work is too large, but because current evidence would be too indirect to prove event ordering unchanged.

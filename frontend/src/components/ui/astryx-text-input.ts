import type { CSSProperties } from 'react';

/** VibeX content-layer surface for Astryx TextInput control chrome. */
export const astryxTextInputSurfaceStyle: CSSProperties = {
  backgroundColor: 'var(--surface-control)',
  borderRadius: 'var(--radius)',
};

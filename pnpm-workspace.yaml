packages:
  - frontend
  - packages/*
  # Duplicate @vibex/plugin-workflow-creator name plus unpublished vendored
  # tarballs; keep it out of the workspace.
  - '!packages/vibex-workflow-mcp'

onlyBuiltDependencies:
  - core-js
  - esbuild

overrides:
  caniuse-lite: 1.0.30001792
